# DECISIONS.md — Decision Log Vokasia

Dipelihara VPM. Setiap keputusan: tanggal, konteks, keputusan, alasan, dampak.

| # | Tgl | Konteks | Keputusan | Alasan | Dampak |
|---|---|---|---|---|---|
| D1 | 2026-07-20 | Nomor bab di instruksi VPM ≠ struktur PRD aktual | `[ASSUMPTION]` mapping: "Bab 15"→Bagian 3, "Bab 5 MoSCoW"→notasi M/S/C Bagian 2, "Bab 6"→matrix 2.3, "Bab 7.3"→2.4, "Bab 13"→NFR-SEC-05 | PRD terkonsolidasi jadi 6 bagian | Nol, kecuali Dev koreksi |
| D2 | 2026-07-20 | Pemilihan IDE (brainstorm tooling) | ~~JetBrains Rider~~ → **direvisi hari yang sama: VS Code + C# Dev Kit** (keputusan Dev) | Gratis; satu jendela backend+frontend+Docker; cukup untuk peran merge/review | Tanpa biaya lisensi; hapus item trial Rider |
| D3 | 2026-07-20 | Cara kerja ENG (ChatGPT) di repo | **Hybrid**: diskusi task di chat, implementasi via Codex di branch `h{N}-eng{X}-{slug}` | Kecepatan + kontrol via review diff; hemat waktu Dev (bottleneck 4–6 jam/hari) | Butuh git init + AGENTS.md (done) |
| D4 | 2026-07-20 | Struktur memory files | Set lengkap: `AGENTS.md` + `SOUL.md` + `DESIGN.md` + `DECISIONS.md` + `TOOLING.md`; **SOUL.md menggantikan persona-*.md** | Satu sumber identitas per file, tanpa duplikasi persona | PRD Lampiran ref persona-*.md obsolete |
| D5 | 2026-07-20 | MCP tambahan | Context7 (pra-sprint) + PostgreSQL MCP (H1) + Playwright MCP (H6) — **sequencing, bukan sekaligus** | Kurangi API halu; inspeksi DB saat review; E2E H7. Sequencing = anti tool-sprawl | Setup bertahap, tidak makan pra-sprint |
| D6 | 2026-07-20 | Pemakaian hallmark | Dijalankan **sekali pra-H1** → isi `DESIGN.md` tokens+voice → beku | Kontrak visual anti-slop untuk ENG-2; sesuai design-in-code PRD 4.1 | Dilarang dipakai mempercantik landing saat sprint |
| D7 | 2026-07-20 | Figma MCP terpasang di Claude Desktop | Ditunda pasca-MVP | PRD 4.1 eksplisit tanpa fase Figma MVP | Nol |
| D8 | 2026-07-20 | Temuan: `D:\Web\Vokasia` **belum git init** padahal scaffold sudah ada | Wajib `git init` + push GitHub private **sebelum sprint** | Mitigasi R5 (rollback) & R6 (commit harian); prasyarat workflow Codex | Langkah 1 di TOOLING.md §3 |
| D9 | 2026-07-20 | codebase-memory-mcp terinstall via npm tapi belum tersambung ke sesi VPM | Jalankan `codebase-memory-mcp install` + restart Claude Desktop & Codex; index perdana H1 | Repo masih kecil — index bermakna mulai H1 | VPM dapat impact analysis saat review |
| D10 | 2026-07-21 | Brainstorm ticketing end-to-end | 21 ticket harian (1 ENG × 1 hari) di `TICKETS.md`; model diferensiasi (Thinking utk auth/async, Terra/Luna utk CRUD/UI, Codex utk semua implementasi); prompt Indonesia + istilah EN; **semua 21 detail dirilis sekaligus** (pilihan Dev, bukan rekomendasi VPM) | Sinkron ritme harian PRD 3.5; kontrak output paksa TASK LIST + WALKTHROUGH + output test (mitigasi R5) | Risiko diterima: ticket H4–H7 provisional, kemungkinan rework bila slip — revisi via checkpoint |
| D11 | 2026-07-21 | Arsitektur eksekusi ticket final (koreksi Dev atas asumsi VPM) | **ChatGPT tetap coder** (GPT-5.4 Thinking / 5.3-Codex / 5.6 Luna per ticket); **Sonnet 5 + Windows-MCP = runner** (buat branch, terapkan kode, jalankan build/test, commit; fix mekanis ≤5 baris & tercatat; logika dikembalikan ke ChatGPT); skala effort runner light→ultra per ticket. Folder `ticket/` (template.md + 21 file, fungsi+parameter+tujuan) = sumber eksekusi; `TICKETS.md` jadi ringkasan/AC referensi. Merevisi D3: Codex CLI tidak dipakai — kode ChatGPT diterapkan runner | Pisahkan desain (ChatGPT) dari eksekusi mekanis (runner) → klaim "sudah ditest" selalu dibuktikan runner, bukan coder | SOUL.md tetap valid (ENG = ChatGPT); batas runner tertulis di ticket/template.md |
| D12 | 2026-07-21 | OmniRoute (AI gateway, localhost:20128) perlu disetup tokenless tp tetap akurat | Kompresi global lossless-first (`session-dedup→ccr→lite→rtk(standard)→headroom→caveman(full)`, Preserve System Prompt=Always, engine lossy/eksperimental off) + kombo `vokasia-quality` priority (Opus 4.8 → Gemini 3.1 Pro High fallback) utk kode non-negotiable; lane hemat via `auto/best-free`/`auto/offline` zero-config utk tugas rutin | RTK menyasar persis output build/test/git command loop runner (D11); kompresi lossless jaga akurasi tanpa reword logic; kedua provider tersambung sudah flat-rate ($0/token) jadi fokus hemat = hemat kuota+latensi, bukan biaya | Kedua langkah kombo lulus tes (dikonfirmasi Dev, fallback awal `gemini-2.5-pro` sempat timeout diganti `gemini-3.1-pro-high`); penyedia gratis tambahan (SiliconFlow/GLM/Kilo) belum disambungkan, butuh kredensial Dev |

| D13 | 2026-07-21 | Scaffold H1 (AppUser) sudah gabungkan field Identity + domain (`FullName, Role, TenantId, IsActive`) dalam satu kelas `AppUser : IdentityUser<Guid>`, bukan `AppUser` + `Student/Teacher/...` terpisah | Diterima apa adanya (retroaktif — keputusan sudah ada di kode sebelum VPM sempat mencatat) | Satu kelas User lintas 7 role lebih sederhana utk MVP; role-specific data (mis. NISN siswa) tetap di `Student` terpisah, ditaut via `Student.UserId` nullable | Konsisten dgn `VokasiaClaimsFactory` & seluruh H2+ RBAC; jangan pecah `AppUser` tanpa migration plan |
| D14 | 2026-07-21 | Eksekusi H1 (VOK-H1-E1/E2/E3): sesi ini tanpa hand-off ChatGPT hidup (D11 butuh coder ChatGPT terpisah, tidak tersedia di sesi non-interaktif ini) | Sonnet 5 merangkap coder+runner utk H1, patuh kontrak output & AGENTS.md; ditemukan+diperbaiki: (1) `using Microsoft.AspNetCore` hilang di `AuthorizationController.cs` (CS1061 `GetOpenIddictServerRequest` — namespace ekstensi OpenIddict 7.6.0 ada di `Microsoft.AspNetCore`, bukan `OpenIddict.Server.AspNetCore`); (2) `AddIdentityCore` didaftar 2× (DI.cs + Program.cs, saling timpa opsi password/lockout) → dikonsolidasi ke `Vokasia.Api/Auth/IdentitySetup.cs` (D13 rujuk ke sini); (3) MinIO client belum terdaftar di DI meski disyaratkan ticket → ditambah `AddMinio` (paket `Minio` 7.0.0 ditambah ke `Vokasia.Infrastructure.csproj`, sudah dipakai di `Vokasia.Api.csproj`, bukan dependency baru); (4) migration `Initial` dibuat; (5) test suite `Vokasia.Tests/Auth/` ditulis+dijalankan sungguhan (bukan diklaim) — sempat gagal krn EF Core mendaftarkan `IDbContextOptionsConfiguration<T>` aditif (Npgsql+InMemory bentrok "two providers registered"), diperbaiki dgn membuang 3 descriptor sebelum re-add InMemory di `VokasiaApiFactory`; (6) `threat-model.md` ditulis | Deviasi dari arsitektur D11 dicatat eksplisit (bukan diam-diam, sesuai SOUL.md hierarki kebenaran) krn tidak ada sesi ChatGPT terpisah yg bisa dipanggil dari lingkungan non-interaktif ini | H1 (M0): build+test backend hijau (10/10 test), `bun run build` frontend hijau, grep hardcode warna nol hasil. **Belum diverifikasi**: `docker compose up -d` 7 service healthy — Docker Desktop sempat crash ("unable to start") + free RAM turun ke ~2GB saat sesi ini, ditunda demi tidak membebani mesin Dev lebih jauh; Dev perlu jalankan `docker compose up -d` manual & lapor hasil sebelum M0 benar-benar dianggap beku |

| D15 | 2026-07-21 | Eksekusi H2-E1 (seeder+endpoint) + separuh H2-E3 (RBAC+tenant middleware), lanjutan sesi D14 | Diimplementasikan+diverifikasi sungguhan terhadap Postgres nyata (bukan cuma compile): `dotnet run -- seed demo` menghasilkan 3 tenant/100 DUDI/900 siswa/900 placement/56673 jurnal/45 siswa RAG-merah/2751 rejected dalam 9,1 detik, idempoten terbukti (re-run → SKIP). Ditemukan+diperbaiki bug nyata: `DateOnly.ToDateTime()` konversi implisit ke `DateTimeOffset` memakai timezone LOKAL mesin (WIB +07:00) — Npgsql `timestamptz` cuma terima offset 0, gagal 100% tanpa perbaikan `new DateTimeOffset(..., TimeSpan.Zero)` eksplisit. RBAC policies (`SaOnly/TenantAdmin/DeptHead+/Teacher+/MentorOwnPlacement/StudentSelf/TenantMember`) & `TenantResolutionMiddleware` diimplementasikan (nama persis sesuai deklarasi ticket VOK-H2-E3 §2) karena endpoint H2-E1 butuh policy nyata, bukan cuma nama string | Bug DateTimeOffset TIDAK akan pernah ketahuan lewat test EF InMemory (provider itu tidak menegakkan aturan UTC-only Npgsql) — murni ketemu karena dijalankan sungguhan ke Postgres real, bukti nilai verifikasi "skeptis thd klaim sudah ditest" (SOUL.md) | **Sengaja BELUM dikerjakan** (dicatat eksplisit, bukan diam-diam): BFF token exchange (route handler Next.js), Redis session store, refresh rotation+reuse detection, magic link mentor — ticket VOK-H2-E3 menandai ini efort **ultra** dgn DoD "clean state penuh: docker compose down -v → up → migrate → seed → seluruh suite security"; menulis kode ini tanpa Redis+HTTP flow nyata utk diverifikasi melanggar bar kualitas ticket itu sendiri. H2-E2 (login UI+guards) juga belum — bergantung kontrak BFF di atas. Docker compose 7-service full health-check (semua service, bukan cuma postgres) JUGA belum diverifikasi penuh — mesin Dev sempat kehabisan RAM (turun ke ~2GB free, Docker Desktop sempat crash "unable to start") saat sesi ini; postgres-only berhasil & stabil, tapi build image api/worker/frontend via compose belum dicoba demi tidak membebani mesin lebih jauh |

| D16 | 2026-07-21 | Eksekusi H2-E2 (login UI+guards), lanjutan sesi D14/D15 | Diimplementasikan+diverifikasi sungguhan (bukan cuma compile): `lib/session.ts` (getSession server/getSessionEdge proxy, cookie `vok_session` httpOnly berisi klaim non-rahasia id/name/role/tenantId — BUKAN token asli, lihat AGENTS.md #3), `lib/roleHome.ts`, `lib/guard.ts` (`resolveGuardDecision` fungsi murni, matrix 4 segment×7 role), `proxy.ts`, `app/login/page.tsx` (form POST), `components/LogoutButton.tsx`, 4 dashboard shell (sapaan nama via session di semua 4; `(school)/app` tambah kartu nyata jml-siswa+placement-aktif dari endpoint H2-E1 asli). **Koreksi mekanis dicatat eksplisit** (bukan diam-diam, SOUL.md): (1) ticket menulis "proxy.ts (root frontend)" + fungsi "middleware()" — istilah lama; Next.js 16 (`next` 16.2.10 di package.json) resmi mengganti `middleware.ts`→`proxy.ts` DAN nama fungsi `middleware`→`proxy` (dikonfirmasi via nextjs.org/docs/app/api-reference/file-conventions/proxy, changelog v16.0.0); lokasi juga dikoreksi ke `src/proxy.ts` (bukan `frontend/proxy.ts`) krn `app/` proyek ini ada di `src/app` — dok Next mewajibkan proxy.ts sejajar `app/`. (2) `Paged<T>`/`PeriodSummary` ditambah di `lib/apiTypes.ts` (tipe FE minimal cermin DTO backend, bukan salinan penuh). Verifikasi nyata: `bun run build` sukses (`"ƒ Proxy (Middleware)"` muncul di output build, bukti proxy kebaca Next.js); `bun test` 14/14 pass (guard.test.ts+session.test.ts, termasuk invariant anti-redirect-loop); **smoke HTTP nyata** (`bun run dev` + curl, mock cookie `vok_session` krn BFF H2-E3 belum ada — sesuai izin eksplisit ticket "verifikasi guard pakai mock session cookie & tandai di laporan"): 8 kombinasi role×segment redirect/lolos persis sesuai spec, `/student` tanpa cookie → 307 ke `/login?error=unauthenticated&next=...`, `/app` dgn cookie TenantAdmin → 200 + sapaan nama nyata + ErrorState graceful (proxy BFF 404 diharapkan, bukan crash) | Deviasi framework-level (bukan desain) — nama file/fungsi salah = proxy senyap tak pernah jalan, itu bug jauh lebih parah drpd mengikuti teks ticket apa adanya. Div-teruji via HTTP asli, bukan cuma unit test, krn ticket sendiri minta bukti "tanpa flash konten" | **Sengaja BELUM dikerjakan** (di luar file-list ticket ini, backend endpoint blm ada): kartu "placement (perusahaan)" milik siswa sendiri di `/student`, "siswa bimbingan" di `/mentor` (`ListPlacements` wajib `periodId`, tanpa filter `studentId`/`mentorUserId`), "daftar tenant" di `/sa` (belum ada `GET /api/tenants`, scope H6-E1) — placeholder asli H1 dipertahankan + komentar TODO eksplisit di kode, BUKAN endpoint baru ditambah diam-diam di ticket FE ini. **Temuan lingkungan** (di luar scope ticket, dilaporkan agar Dev sadar): (a) seluruh working tree menunjukkan diff CRLF↔LF masif (~200 file) yang TIDAK disebabkan sesi ini — sudah ada sebelum sesi H2-E2 dimulai; 17 file ticket ini dinormalisasi ke LF (cermin blob HEAD) sebelum commit agar diff bersih, sisanya dibiarkan (perlu `.gitattributes` + renormalize sekali oleh Dev, di luar wewenang runner); (b) `.git/index.lock` & `.git/objects/*/tmp_obj_*` berulang kali gagal ke-unlink dari sisi mount Linux (`Operation not permitted`) — diatasi via PowerShell `Remove-Item` langsung tiap kali macet; (c) proses `next dev`/Turbopack (dari smoke test sendiri) sempat tertinggal (child process node.exe tidak ikut mati saat parent `bun` di-`Stop-Process`) dan mengunci file `frontend/src/**` sampai membuat `git reset --hard`/`checkout` gagal separuh jalan — diperbaiki dgn `Stop-Process` eksplisit ke PID node yg tersisa lalu `git update-ref`+`git reset` (mixed, tanpa sentuh working tree) alih-alih `reset --hard`; tidak ada data hilang (working tree sudah benar dari awal, cuma index git yg telat sinkron); (d) folder `Vokasia/` (repo git terpisah, README+LICENSE MIT beda, tampaknya percobaan awal proyek yg tertinggal) ada di root, untracked, TIDAK disentuh — perlu keputusan Dev mau dihapus/dipindah atau dibiarkan |

| D17 | 2026-07-21 | Eksekusi H2-E3 (BFF token exchange + Redis session + refresh rotation + revocation + audit), lanjutan sesi D14/D15/D16 — masih tanpa hand-off ChatGPT hidup (D14) | Diimplementasikan+diverifikasi lewat HTTP nyata penuh (curl+PowerShell, backend `dotnet run` + frontend `bun run dev` + Postgres/Redis docker nyata — bukan mock/in-process saja): `frontend/src/app/api/auth/{login,callback,session,logout}/route.ts`, `app/api/proxy/[...path]/route.ts` (`proxyWithBearer`, retry-once-after-refresh), `lib/{pkce,redis,bffSession,refresh}.ts` (state+PKCE Redis TTL 5 menit sekali-pakai, sesi Redis TTL 14 hari cermin refresh lifetime, keluarga sesi per-user via SET `user-sessions:{userId}` untuk revoke-semua), `backend/.../Auth/AccountEndpoints.cs` (`/account/login` GET form + POST — gap H1-E3 yang baru ketahuan), `AuditEndpoints.cs` (`POST /api/audit`). Login→authorize→PKCE→token→session→dashboard-data-asli→logout→revoke-terbukti dijalankan end-to-end LEWAT DUA TENANT BERBEDA (admin@10000001 & admin@10000002) — identitas+nama tenant benar per sesi, isolasi dikonfirmasi juga langsung ke Postgres (query manual: 300/300/300 siswa & placement aktif per tenant, simetris by design bukan bocor). **8 bug nyata ditemukan+diperbaiki** (semua lewat perilaku HTTP asli, bukan cek konfigurasi statis — 5 pertama dari paruh awal sesi ini sebelum context compaction, 3 terakhir sesudahnya): (1) OpenIddict menolak semua `/connect/*` lewat HTTP polos ("HTTPS only") → `DisableTransportSecurityRequirement()` khusus `env.IsDevelopment()`; (2) `invalid_scope` utk "profile"/"api" → scope harus didaftarkan eksplisit ke `IOpenIddictScopeManager`, terpisah dari permission client; "profile" di-drop dari scope yang diminta (tak dipakai, klaim tak digerbangi scope); (3) client BBF `vokasia-bff` dari seed H1-E3 kekurangan permission `Revocation` & idempotensi lama ("return early bila ada") berarti tak pernah ter-update → `SeedOAuthClientsAsync` diubah jadi create-or-**update** (dibuktikan lewat SQL `UPDATE` nyata di log restart); (4) `Challenge()` bawaan `AuthorizationController.Authorize()` menghasilkan 401+header Location (BUKAN 3xx sungguhan) — browser/curl tak mengikuti 401 otomatis, flow interaktif macet total → diganti `Redirect()` manual; (5) `/account/login` (LoginPath sejak H1-E3) tidak pernah dibuat sama sekali → `AccountEndpoints.cs` ditulis penuh (form HTML dev-only + POST validasi password + `SignInAsync` scheme "Cookies" eksplisit + 303 See Other ke returnUrl, pola Post/Redirect/Get baku); (6) access token OpenIddict defaultnya **terenkripsi** (JWE 5-segmen), bukan cuma bertanda tangan (JWS 3-segmen) — `decodeJwtPayload` BFF gagal `JSON.parse` (ciphertext dikira JSON) krn BFF (proses Next.js terpisah) tak punya kunci privat dekripsi apa pun (beda dari resource server, proses yang sama dgn sertifikat yang sama) → `options.DisableAccessTokenEncryption()` (refresh token TETAP reference/opaque, tak terpengaruh); (7) `lib/fetcher.ts` (kerangka H1) menembak URL RELATIF `/api/proxy/*` — benar utk fetch browser, SALAH dipanggil dari Server Component (`(school)/app/page.tsx`) yang jalan di proses Node SSR: fetch server tidak punya origin implisit (URL relatif gagal di-parse → exception tertelan try/catch generik, dashboard SELALU jatuh ke ErrorState apa pun status proxy sebenarnya) DAN tidak otomatis membawa cookie request masuk → `fetcher.ts` ditulis ulang: baca sesi langsung via `next/headers` cookies() (valid krn HANYA dipanggil dari Server Component), panggil backend LANGSUNG dgn Bearer (skip hop diri sendiri), retry-once-after-refresh sendiri (duplikasi sadar dari `proxyWithBearer`, didokumentasikan di komentar, bukan diekstrak demi tak mengutak-atik route yang sudah lolos verifikasi); (8) **bug paling parah**: `IdentitySetup.cs` mendaftarkan `AddAuthentication(CookieAuthenticationDefaults.AuthenticationScheme)` sbg default scheme APLIKASI PENUH sejak H1-E3 — akibatnya SEMUA endpoint resource (`RequireAuthorization(RbacPolicies.*)` tanpa `AddAuthenticationSchemes` eksplisit, yaitu literally semua endpoint H2-E1: students/periods/placements/dst.) diam-diam diautentikasi lewat handler Cookie SAJA, mengabaikan header `Authorization: Bearer` sepenuhnya — token JWT valid pun dibalas 302 ke `/account/login` (perilaku "challenge" Cookie thd request tanpa cookie), padahal seluruh pola BFF ticket ini BERGANTUNG pada Bearer bekerja di endpoint resource. Lolos tanpa terdeteksi di H1-E3/H2-E1 krn test yang ada set `ClaimsPrincipal` cookie langsung in-process, tak pernah lewat header Bearer sungguhan → default scheme diubah ke `OpenIddictValidationAspNetCoreDefaults.AuthenticationScheme`; Cookie TETAP terdaftar+dipakai tapi SELALU via nama skema eksplisit (kedua pemanggil, `AuthorizationController` & `AccountEndpoints`, sudah begitu dari awal — nol dampak ke keduanya, dikonfirmasi 14 test lama tetap hijau). **1 kecurigaan bug yang TERNYATA BUKAN bug server**: dance `curl -L` mengikuti 303 dari POST `/account/login` awalnya tampak resend sbg POST tanpa body/Content-Type (ditolak OpenIddict "mandatory Content-Type missing") — didiagnosis tuntas via GET manual eksplisit ke URL Location yang SAMA (sukses, 302 dgn code asli): akar masalah ada di *test script* (`curl -L` tidak mengonversi metode POST→GET stlh 303 sesuai ekspektasi awal saya di lingkungan ini), BUKAN di server — browser sungguhan mengikuti semantik Post/Redirect/Get standar. 303 (bukan 302) dipertahankan sbg pilihan HTTP yang lebih benar/tak-ambigu, tapi didokumentasikan jujur sbg *bukan* perbaikan bug server, murni kualitas rekayasa. Regresi bug (8) dikunci via test baru `BearerAuthenticationTest.cs` (3 test: Bearer-token valid→200 di endpoint resource sungguhan; tanpa-token→ditolak; refresh-token-setelah-revoke→ditolak) — **dibuktikan tuntas merah dulu** (revert 1 baris → `Expected: OK, Actual: Found`, persis prediksi) sebelum dikembalikan hijau, sesuai PROMPT D template.md. Total 17/17 test backend hijau (14 lama + 3 baru), 14/14 test frontend hijau (fetcher.ts diubah total tanpa test dedicated, tak ada regresi test lain), `bun run build` hijau (route `/api/auth/*` & `/api/proxy/*` muncul sbg ƒ Dynamic) | Kedelapan bug HANYA muncul lewat perilaku HTTP sungguhan (redirect asli, token asli, dua proses Node+dotnet terpisah, dua tenant nyata) — cerminan tepat SOUL.md "skeptis thd klaim sudah ditest": setiap satu bug ini akan LOLOS test in-process/mock yang lebih dangkal. Bug (8) khususnya adalah kelas silent-regression yang paling berbahaya (tak ada error/crash, cuma redirect yang terlihat "masuk akal" sekilas) — alasan utama kenapa test regresi ditulis dgn dance OAuth PENUH via `HttpClient` TestServer, bukan cuma assert nilai `DefaultScheme` di options | **Sengaja BELUM dikerjakan** (eksplisit diizinkan ticket sendiri, "boleh geser pagi H3 — lapor, jangan diam"): magic link mentor (`CreateMentorInvite`/`ValidateMagicToken`/`ExchangeMagicToken`, §3 ticket) — nol baris ditulis, bukan separuh-jalan. **Test §4 ticket yang belum**: `RbacPolicyTests` (matrix RBAC penuh lintas 7 role — RBAC POLICY DEFINITION sendiri sudah lolos scrutiny tak langsung via `BearerAuthenticationTest` TenantMember, tapi belum ada test per-role granular), `MagicLinkTests` (menunggu fitur di atas). DoD ticket "clean state penuh: docker compose down -v → up → migrate → seed → seluruh suite security" **belum** — hanya Postgres+Redis (bukan rabbitmq/minio/full app containers) yang jalan, keputusan scoping RAM yang sama dgn sesi H1/H2-E1 (D14/D15); Redis sempat mati bareng Postgres di tengah sesi ini (kedua container berhenti tanpa root cause yang diselidiki — kemungkinan restart Docker Desktop di luar kendali sesi), dibangunkan ulang via `docker compose up -d postgres redis`, data Redis/Postgres survive (volume tidak hilang). File `frontend/.env.local` (gitignored) berisi `SESSION_SECRET` dev yang di-generate sesi lalu — TIDAK di-regenerate, TIDAK dicommit, konsisten dgn AGENTS.md #4 |

| D18 | 2026-07-21 | Dev minta jalankan skill "hallmark" utk mengisi DESIGN.md (Tokens+Voice, TODO sejak D6/D7 — hallmark tak pernah benar-benar dijalankan pra-H1 meski direncanakan) | "hallmark" **bukan** skill Cowork bawaan sesi ini (`Skill` tool → `Unknown skill: hallmark`, dicoba 2× — sebelum & sesudah instalasi). Dev minta eksplisit `npx skills add nutlope/hallmark`. Dijalankan (`-y -g`, sempat 2× gagal krn prompt interaktif tak bisa dijawab lewat PowerShell non-TTY) — berhasil terinstal ke `~/.agents/skills/hallmark` + symlink `~/.claude/skills/hallmark` (lokasi **Claude Code**, path GLOBAL user, BUKAN cache plugin Cowork per-sesi yang mengisi `<available_skills>`). Dicoba invoke `Skill` lagi pasca-instal → **tetap** `Unknown skill` — dikonfirmasi keterbatasan teknis nyata (skill listing Cowork tetap dari cache sesi, tidak scan ulang `~/.claude/skills` di tengah sesi), bukan cuma masalah izin. Jalan keluar: baca `SKILL.md` (558 baris) + reference terpakai (`design-md.md`, `color.md`, `typography.md`, `copy.md`, `layout-and-space.md`, `motion.md`) langsung via `Windows-MCP PowerShell Get-Content` (Cowork `Read` tool tak bisa jangkau path itu — di luar folder tersambung), lalu **terapkan metodologinya manual** (bukan invoke skill formal) utk mengisi DESIGN.md + `globals.css`. Keputusan desain nyata yang diambil: **genre modern-minimal** (institusional/B2B multi-tenant, bukan konsumer — cocok sinyal SaaS/enterprise/platform hallmark); **tone utilitarian** (cermin brief DESIGN.md sendiri: "app fungsional PKL, bukan landing page marketing"); **theme custom-tuned** (BUKAN salah satu 20 tema katalog) — pre-flight scan (Langkah 0 hallmark, WAJIB baca kode dulu sebelum putuskan apa pun) menemukan Geist+Geist Mono sudah dipakai sejak H1-E2 lewat `next/font`; prioritas #1 pre-flight adalah "preserve font stack", jadi font TIDAK diganti — hanya palet warna yang di-tuning penuh ke OKLCH. **Anchor hue 222° (teal-biru)** dipilih eksplisit BUKAN biru generik ~250–265° yang menurut `color.md` hallmark sendiri dipakai hampir semua produk AI/SaaS ("Most AI-generated UI fails on colour. It picks blue.") — 222° tetap terbaca tepercaya/institusional, beda jelas dari hue RAG (hijau~155°/kuning~75-85°/merah~25°) agar warna brand tak pernah rancu dgn status di dashboard padat-data, chroma rendah-sedang (0,10-0,16) sesuai tone utilitarian bukan luxury/playful. **Verifikasi kontras dilakukan programatik, bukan diasumsikan**: ditulis konversi OKLCH→sRGB→luminans-relatif→rasio WCAG (Python, `mcp__workspace__bash`) dan dijalankan thd seluruh pasangan teks/latar (body/helper/card/link/tombol/border/focus-ring, light+dark, RAG ×2 mode) terhadap target `color.md` (body ≥4,5:1 min/7:1 target, batas UI/border ≥3:1). **Temuan nyata**: token border/`--color-border` LAMA (hex placeholder `#dde1e7`/`#2b323d` dari H1-E2) gagal kontras sungguhan (1,31:1 terang / 1,38:1 gelap) — jauh di bawah 3:1 WCAG 1.4.11 non-text; ini bug aksesibilitas nyata yang sudah ada di produksi, bukan sekadar pilihan estetik hallmark. Nilai baru (`oklch(62% 0.016 222)` terang / `oklch(52% 0.018 222)` gelap) di-iterasi lewat script sampai lulus 3:1 (hasil 3,42:1/3,54:1). Seluruh token lain (ink/ink-muted/accent/accent-ink/focus/RAG×2 mode) diverifikasi lulus PERTAMA kali tanpa perlu iterasi. Motion: token `--ease-*`/`--dur-*` ditambah (net-new, tanpa pustaka animasi — pre-flight scan `package.json` konfirmasi tak ada `framer-motion`/`gsap`/dst → stance "motion-cut" per hallmark sendiri, sejalan persis dgn batasan beku DESIGN.md "minimal, tanpa animasi berat di 3G"). **Tidak ada rename token** — semua nama `--color-*`/`--space-*`/`--radius-*` yang sudah dipakai komponen (Card/Button/StatusBadge/dst, H1-E2) dipertahankan persis, hanya NILAI yang berubah (hex→oklch) — zero risiko breaking change di pemakai token | Instalasi via `npx` di sesi Cowork sandboxed TIDAK terintegrasi dgn sistem skill Cowork sendiri (keduanya baca dari lokasi berbeda) — didokumentasikan transparan drpd berpura-pura skill itu "aktif". Keputusan desain (anchor hue, genre, tone, theme route) SEMUA dari penerapan sadar metodologi hallmark thd konteks proyek NYATA (pre-flight scan kode asli, brief DESIGN.md yg sudah beku), bukan tebakan/template generik dari skill itu sendiri. Verifikasi kontras terprogram (bukan visual/asumsi) konsisten dgn pola sesi ini (D14-D17: "skeptis thd klaim sudah ditest") | **STATUS PROPOSAL, BUKAN BEKU** — DESIGN.md baris status diubah eksplisit jadi "menunggu approval Dev" (bukan diam-diam ditandai beku), sesuai aturan file itu sendiri ("Setelah terisi dan disetujui Developer → beku; perubahan = change control PRD 3.6"). Dev perlu review lalu ganti baris status jadi BEKU. Verifikasi lanjutan: `bun run build`+`bun test` dijalankan pasca-perubahan (lihat commit) — TIDAK ada pengecekan visual manusia/screenshot browser sungguhan thd token baru (di luar kemampuan sesi ini scr wajar utk perubahan token murni, bukan komponen baru); Dev disarankan lihat langsung di `bun run dev` sebelum approve. `--color-primary-muted` dark-mode BELUM diverifikasi kontras (dipakai sbg background tint, bukan teks — risiko rendah, tapi belum dicek programatik sama spt yang lain, dicatat sbg gap eksplisit) |

| D19 | 2026-07-21 | Dev pilih "Redesign halaman Vokasia" (dari 3 opsi: redesign nyata / laporan status / deskripsi rencana saja) sbg cara konkret memperlihatkan metodologi hallmark, thd `/app` (dashboard Admin Sekolah) — dipilih drpd halaman lain krn sudah punya data KPI nyata (H2-E1: 300 siswa, 300 placement aktif) dan sidebar-nya sudah dekat bentuk N3 side-rail hallmark (refinement, bukan penggantian asing) | Dibaca dulu (pre-flight, wajib): `macrostructures.md` index, `04-stat-led.md`, `component-cookbook.md` (routing nav per genre), `interaction-and-states.md` — via `Windows-MCP PowerShell` (sama spt D18, Cowork `Read` tak jangkau path global hallmark). **Component-scope diterapkan, BUKAN full macrostructure** — brief tetap "app fungsional, bukan landing" (DESIGN.md §Aturan pemakaian butir 3), jadi TANPA hero/footer archetype/apparatus marketing; hanya 2 disiplin universal hallmark yang diterapkan: (1) **hierarki kartu KPI** — 2 kartu SAMA besar berdampingan (equal-card default, ditandai hallmark sbg tanda AI-generik) diganti asimetri nyata: "Placement Aktif" jadi lead (`md:col-span-2`, angka `text-5xl`) krn itu metrik operasional yg berubah harian (paling sering dicek admin), "Jumlah Siswa" jadi pendukung (`col-span-1`, `text-3xl`); pola angka+kata wajib dari macrostructure Stat-Led ("angka besar tak pernah berdiri sendiri sbg headline") diterapkan di skala kartu; TANPA animasi count-up (motion.md hallmark sarankan itu utk hero marketing — kontradiksi stance motion-cut beku DESIGN.md utk app fungsional, sengaja dilewati, angka tampil langsung); nol metrik baru/karangan (2 angka nyata persis sama dari `loadStats()`, hanya presentasi berubah). (2) **8-state interaksi** (`interaction-and-states.md`) — 2 celah nyata ditemukan: (a) `Button.tsx` TANPA focus-visible sama sekali (andalkan outline default browser); (b) nav sidebar (`layout.tsx` lama) hanya hover (2 dari 8 state), TANPA focus-visible DAN tanpa indikator halaman-aktif sama sekali (tak ada `aria-current`). Kedua celah ditambal pakai `--color-focus` (token sudah ada sejak D18, sebelumnya **tak dipakai satu komponen pun** — celah nyata, bukan kosmetik: navigasi keyboard-only sebelumnya tak pernah melihat posisi fokus). Nav-current pakai teknik "reserved slot" (`border-l-2 border-transparent` di semua item, ganti jadi `border-primary` hanya saat aktif) → **nol layout shift** saat state berubah, sesuai "no-layout-shift rule" hallmark. Extraksi `SidebarNav.tsx` (client component terisolasi, pola sama `OfflineBanner.tsx`) krn `usePathname` butuh "use client" — `SchoolLayout` induk tetap Server Component (AGENTS.md #10). **Deviasi nav archetype dicatat sadar**: genre modern-minimal hallmark routing table default ke N1b (top bar), side-rail (N3) bahkan tak masuk daftar "acceptable" utk modern-minimal (itu default genre docs/reference) — tapi tabel itu dikalibrasi utk halaman marketing; app internal ini py 8 tujuan persisten terautentikasi, pola mapan justru side-rail (Linear/Vercel/Stripe dashboard); N1b py 8 item butuh dropdown/sesak. Keputusan: pertahankan side-rail, terapkan HANYA disiplin interaksinya, bukan ganti archetype. **Ditemukan+diperbaiki sbg bonus**: `Button.tsx` working-tree CRLF vs HEAD blob LF (line-ending drift environment, bukan isi) — dinormalisasi ke LF HANYA utk file ini (diff jadi bersih ~13 baris, bukan 121) sblm commit; ditemukan JUGA saat `git status` bahwa **hampir seluruh repo** (backend .cs, migrations, docker-compose.yml, dst — file yg TIDAK disentuh sesi ini) muncul sbg "modified" dgn pola sama (CRLF working-tree vs LF HEAD) — **TIDAK diperbaiki** (di luar scope tiket ini, blast-radius besar, perlu keputusan terpisah Dev: mis. `.gitattributes` root + renormalize). Ditemukan juga direktori `Vokasia/Vokasia/` (nested clone terpisah, remote `github.com/IvanoWisnuBudiLaras/Vokasia`, 1 commit "Initial commit") — untracked, tak disentuh, dilaporkan ke Dev sbg temuan, bukan dihapus sepihak | Sesi ini bukan invoke skill formal (keterbatasan D18 tetap berlaku), tapi penerapan sadar 2 disiplin hallmark yg PALING relevan+universal (hierarki visual, kelengkapan state interaktif) thd 1 halaman nyata yg berfungsi, dgn tiap penyimpangan dari default hallmark (component-scope vs full macrostructure, side-rail vs N1b) dinyatakan eksplisit + alasannya — konsisten pola transparansi sesi ini (D14-D18) | KPI dashboard nyata terlihat berhierarki (bukan 2 kartu kembar), navigasi keyboard-only sekarang punya fokus terlihat + tahu halaman mana yang aktif — perbaikan a11y nyata, bukan kosmetik semata. Kontras 2 pasangan BARU yg diperkenalkan redesign ini (blm pernah dicek D18) sudah diverifikasi programatik (Python OKLCH→sRGB→WCAG, sama metode D18) pasca commit: fokus-ring `--color-focus` thd bg `--color-primary-muted` (nav aktif) = 3,28:1 terang/6,83:1 gelap (lulus ≥3:1); teks `--color-primary` thd bg `--color-primary-muted` (label nav aktif) = 5,10:1 terang/5,84:1 gelap (lulus ≥4,5:1); label eyebrow `--color-ink-muted` thd `--color-surface` (kartu KPI) = 6,69:1 terang/8,44:1 gelap (lulus ≥4,5:1). **Belum diverifikasi**: screenshot visual browser sungguhan (di luar kemampuan wajar sesi tanpa dev-server+browser interaktif berjalan penuh) — Dev disarankan lihat langsung di `bun run dev` sebelum approve, sama spt catatan D18. **Belum ditangani (flagged, bukan scope tiket ini)**: `Input.tsx`/`Textarea.tsx` masih pakai `focus:outline-primary` (bukan `focus-visible:`, bukan token `--color-focus`) — tak dirender di halaman `/app` dashboard jadi di luar scope redesign ini, dicatat sbg follow-up |

| D20 | 2026-07-21 | Dev diberi tahu status DESIGN.md ("PROPOSAL — menunggu approval") + isi lengkapnya (Tokens/Voice hasil D18, penerapan komponen `/app` dashboard hasil D19) via chat, diminta approve | Dev balas eksplisit **"approve"** — dicatat sbg persetujuan formal, bukan diasumsikan. Baris status `DESIGN.md` diubah dari "PROPOSAL — menunggu approval Dev" jadi **"BEKU"**, sesuai aturan file itu sendiri ("Setelah terisi dan disetujui Developer → beku"). Yang ikut disetujui: seluruh isi §Tokens (warna OKLCH anchor hue 222°, tipografi Geist dipertahankan, spacing 4pt, motion-cut) + §Voice & copy (D18), DAN penerapan nyata di `/app` dashboard — hierarki kartu KPI + 8-state interaksi nav/tombol (D19) | **PRD 3.6 change control kini AKTIF** utk `DESIGN.md` sejak titik ini (bukan sebelum, sesuai aturan filenya sendiri) — perubahan token/voice selanjutnya WAJIB via entry DECISIONS.md baru, bukan edit senyap. Implikasi turunan: `globals.css` (sumber kebenaran token) dan komponen `Button.tsx`/`SidebarNav.tsx`/dst yang sudah memakai token ini kini dianggap kontrak stabil, bukan lagi draf |

| D21 | 2026-07-21 | Ticket VOK-H2-E3 §3 (magic link mentor) sengaja ditunda sesi sebelumnya (D17: scope note ticket sendiri "boleh geser pagi H3 — lapor, jangan diam", demi memprioritaskan BFF/RBAC/refresh yg lbh mendesak). Dev minta lanjutkan semua tugas tersisa | Diimplementasikan penuh end-to-end, bukan separuh-jalan: `MentorInvite` entity (migration `AddMentorInvite`, diterapkan ke Postgres nyata via `dotnet ef database update`, tabel+2 index terverifikasi lewat SQL asli yg dieksekusi) + `MagicLinkService` (`CreateInviteAsync`/`ValidateAsync`/`ExchangeAsync`, nama persis cermin ticket §3 `CreateMentorInvite`/`ValidateMagicToken`/`ExchangeMagicToken`) + endpoint REST (`POST /api/mentor-invites` policy TeacherPlus, `GET /api/mentor-invites/validate` anonim BY DESIGN — tak mengonsumsi token) + grant OpenIddict kustom `urn:vokasia:params:oauth:grant-type:magic-link` (cabang baru di `AuthorizationController.Exchange()`, BUKAN endpoint REST terpisah — exchange HANYA lewat `/connect/token`) + route BFF `app/api/auth/magic-link/route.ts` (pola sama `callback/route.ts` tp tanpa PKCE/state, klik tunggal non-interaktif) + halaman publik `app/mentor-invite/page.tsx` (sengaja di luar semua route group terjaga; dikonfirmasi matcher `proxy.ts` `["/sa/:path*","/app/:path*","/mentor/:path*","/student/:path*"]` tak menjangkau segment top-level `/mentor-invite`, jadi aman tanpa ubah matcher). 5 `MagicLinkTests` (happy path create→exchange; dipakai 2×→ditolak; kedaluwarsa→ditolak; token sampah→ditolak; validate tak mengonsumsi token) — semua hijau, diverifikasi PROMPT D (comment `invite.UsedAt = DateTimeOffset.UtcNow;` → 2 test terkait merah PERSIS prediksi → dikembalikan → hijau lagi) | Token diterbitkan lewat SATU jalur `SignIn`+`VokasiaClaimsFactory` yg identik dgn grant lain (authorization_code, refresh_token) — bukan sesi ad-hoc paralel; pelajaran D17 soal bahaya default-scheme diam-diam ditegakkan lagi di sini scr sengaja. Token mentah (32 byte acak, base64url) di-hash SHA-256 sebelum disimpan (`MentorInvite.TokenHash`) — penerima dapat token asli, DB cuma simpan hash — prinsip identik refresh token reference OpenIddict sendiri (bocor DB ≠ token siap pakai). Desain 2-langkah validate(non-consuming)-lalu-exchange(consuming) sengaja melindungi dari email/link-preview scanner yg otomatis mengunjungi URL di email & membakar token sekali-pakai sebelum penerima asli sempat klik — URL yg ditaruh di badan email HANYA halaman konfirmasi (`/mentor-invite?token=`), BUKAN URL exchange (`/api/auth/magic-link?token=`) itu sendiri; exchange baru terjadi lewat klik tautan sungguhan (`<a href>`) di halaman konfirmasi itu, yg butuh manusia nyata | Vertical slice VOK-H2-E3 §3 selesai penuh. **Sengaja belum dikerjakan** (sesuai scope ticket sendiri, H4-E3): pengiriman email sungguhan — mode dev cuma log magic link URL via `ILogger` + kembalikan langsung di response `POST /api/mentor-invites` (dipakai guru/admin salin-tempel manual sampai infra email H4-E3 ada) |
| D22 | 2026-07-21 | Melanjutkan sisa test §4 ticket VOK-H2-E3 (`RbacPolicyTests`, `RefreshRotationTests`, `RevocationTests`, ekstrak `AuthTestHelpers` dari `BearerAuthenticationTest` krn 3 file baru butuh helper login+seed identik) — `RefreshRotationTests.OldRefreshToken_AfterRotation_IsRejected` GAGAL pada run sungguhan pertama: refresh token lama yg baru saja dipakai sekali utk rotasi (dapat token baru B) ternyata TETAP diterima (200 OK) dipakai ulang, bukan ditolak (`Assert.Equal` gagal, `Expected: BadRequest, Actual: OK`) | Diselidiki lewat sumber primer, bukan tebakan (pola PROMPT D sesi ini): dugaan awal "rotasi tak aktif" → dicoba `options.UseRollingTokens()` → **GAGAL build** (`CS1061`, API itu tak ada di `OpenIddictServerBuilder` versi 7.6.0 terpasang — API OpenIddict versi lama, dikonfirmasi via error compiler nyata, ditarik lagi). Akar masalah sesungguhnya dikonfirmasi via dokumentasi resmi (documentation.openiddict.com/configuration/token-storage) + jawaban maintainer kevinchalet di openiddict-core#1274: rotasi refresh (redeem token lama + terbitkan token baru) SUDAH default sejak OpenIddict 3.0+ (konfigurasi proyek ini SUDAH benar sejak awal) — TAPI ada **"reuse leeway" default 30 DETIK**, resmi didesain utk mentolerir request konkuren/retry jaringan: dalam jendela itu, token yg BARU SAJA diredeem tetap diterima dipakai ulang tanpa penolakan. Test menembak ulang dlm hitungan milidetik setelah rotasi pertama — jauh di bawah 30 detik, persis di dalam jendela toleransi itu, bukan di luarnya. `options.SetRefreshTokenReuseLeeway(TimeSpan.Zero)` ditambahkan ke `OpenIddictSetup.cs` (persis sesudah `UseReferenceRefreshTokens()`) — tolak reuse SEKETIKA, nol toleransi | Model ancaman AC VOK-H2-E3 §4 ("reuse refresh lama → seluruh keluarga sesi tercabut", ditangkap `revokeAllSessionsForUser` di `frontend/src/lib/refresh.ts`, kode sudah ada sejak D17) adalah skenario **pencurian token** (attacker replay refresh lama setelah korban sendiri sudah lanjut pakai yg baru) — beda jenis risiko dari retry-jaringan-konkuren yg leeway 30 detik OpenIddict memang ditujukan utk. Toleransi 30 detik berarti pencuri token dapat jendela hampir setengah menit memakainya tanpa terdeteksi sama sekali — bertentangan langsung dgn niat AC. **Trade-off disadari & dicatat eksplisit, bukan diam-diam**: klien SAH yg mengalami hiccup jaringan tepat sesudah rotasi (mis. respons token baru gagal sampai ke BFF krn timeout, BFF retry otomatis pakai refresh token yg SAMA yg sebenarnya sudah terpakai di server) kini akan ditolak keras juga (harus login ulang), bukan ditoleransi diam-diam spt sebelumnya — diterima krn model ancaman proyek ini eksplisit minta deteksi-reuse instan, dan "cabut semua sesi" pada reuse toh sudah berarti UX login-ulang, cuma dipicu 1 request lebih cepat dari yg leeway lama izinkan | `RefreshRotationTests` 2/2 hijau, diverifikasi PROMPT D penuh (merah nyata di bawah leeway default 30 detik — kegagalan asli yg memicu investigasi ini, BUKAN direkayasa — → hijau sesudah `SetRefreshTokenReuseLeeway(Zero)`). Suite penuh `dotnet test` 34/34 hijau — sekalian mengonfirmasi **PERTAMA KALI** `RbacPolicyTests` (7 test: matrix DeptHeadPlus/TenantAdminOnly/TenantMember lintas Teacher/Student/DeptHead/TenantAdmin via Bearer token sungguhan dance PKCE penuh, + 2 unit test langsung `PlacementScopeHandler` via `AuthorizationHandlerContext`) dan `RevocationTests` (2 test: login ditolak utk user nonaktif; refresh ditolak stlh dinonaktifkan pasca-login sah) — keduanya BELUM PERNAH dijalankan sebelum sesi ini, dan keduanya lolos tanpa perlu perbaikan. `AuthTestHelpers.cs` (ekstraksi murni mekanis dari `BearerAuthenticationTest.cs`: `GeneratePkce`/`SeedUserAsync`/`LoginAndExchangeAsync`/konstanta + `AttemptLoginFormAsync` baru utk kasus login-ditolak) — 3 assertion lama `BearerAuthenticationTest` tetap tak berubah & tetap hijau. Ini **celah keamanan nyata PRA-ADA** (bukan bug kode sesi ini — `OpenIddictSetup.cs` milik H1-E3/D14, jauh sebelum magic link) yg baru ketahuan krn test §4 ticket ini akhirnya benar² ditulis+dijalankan sungguhan thd server nyata — bukti lain nilai "skeptis thd klaim sudah ditest" (SOUL.md): tanpa test HTTP nyata ini, jendela toleransi 30 detik utk token curian yg direplay akan tetap tak disadari selamanya, DAN mekanisme `revokeAllSessionsForUser` BFF (yg sudah ditulis sejak D17) tak akan pernah benar² terpicu oleh skenario yg jadi alasan ia ditulis |

| D23 | 2026-07-21 | Dev mengizinkan mencoba Gate M0's docker-compose 7-service health-check yg berulang kali ditunda sejak D14/D15/D17 (RAM Dev sempat turun ke ~1-1,8GB free, Docker Desktop di-alokasi cuma 2 core/2GB — dikonfirmasi Dev sendiri stlh build `api` macet TOTAL 20+ menit tanpa progres apa pun, Build Cache docker persis sama byte-nya di 3 pengecekan berturut). Dev menaikkan alokasi Docker Desktop ke 8 core/6GB/swap 4GB, minta lanjutkan | Build ulang per-service (bukan `up -d` sekaligus, demi jejak RAM lebih kecil tiap langkah) membongkar **3 bug nyata pra-ada** di infra compose yg tak pernah ketahuan krn compose penuh tak pernah berhasil dijalankan sebelumnya: (1) `Vokasia.Api/Dockerfile` & `Vokasia.Worker/Dockerfile` merujuk `backend.sln`/`Vokasia.sln` yg TAK PERNAH ADA — file solusi sesungguhnya `Vokasia.slnx` (format XML SDK baru) → `dotnet restore` selalu gagal exit 1 sejak Dockerfile ditulis; diperbaiki jadi `dotnet restore Vokasia.slnx` di keduanya. (2) Healthcheck `api` (`wget -qO- http://localhost:8080/health/ping`) SELALU gagal — image final `mcr.microsoft.com/dotnet/aspnet:10.0` (Debian, bukan Alpine) TIDAK menyertakan `wget`/`curl` bawaan (`exec: wget: executable file not found`), padahal app-nya sendiri sehat (log normal, listening 8080) — container tetap berstatus "unhealthy" selamanya meski aplikasinya baik-baik saja; diperbaiki dgn `apt-get install -y --no-install-recommends wget` di stage final. (3) Healthcheck `frontend` (`wget` juga) gagal dgn sebab BERBEDA & lebih dalam: `oven/bun:1-slim` JUGA tanpa wget (diganti healthcheck native `bun -e "fetch(...)"`, tanpa apt-get, jaga image tetap ringan) — TAPI setelah itu tetap gagal connect krn Next.js standalone `server.js` baca env var `HOSTNAME` bawaan Docker (otomatis di-set ke container ID) sbg alamat bind, BUKAN `0.0.0.0` — server cuma listen di alamat resolve container-ID itu, `localhost` DARI DALAM container sendiri (yg dipakai healthcheck) tak nyambung ("Unable to connect"), walau port-mapping host→container kebetulan tetap jalan (proxy Docker target IP asli container, sama dgn yg dipakai server) — diperbaiki via `ENV HOSTNAME="0.0.0.0"` eksplisit di stage final `frontend/Dockerfile`, override bawaan Docker. Ketiganya diverifikasi satu-satu (bukan sekaligus lalu diasumsikan benar): tiap fix → rebuild service itu SAJA → `docker compose ps` cek status → lanjut ke service berikutnya | Ketiga bug ini HANYA bisa ketahuan lewat percobaan build+run compose SUNGGUHAN, persis alasan gate ini ditunda 3× sebelumnya (D14/D15/D17: "RAM tak cukup, ditunda demi tak membebani mesin Dev") — begitu akhirnya benar-benar dicoba (dgn izin eksplisit Dev, dgn Docker Desktop diberi resource lebih besar), semua muncul sekaligus. Ini konsisten pola sesi ini (D14-D22): kode yg "kelihatan benar" (Dockerfile ditulis wajar, healthcheck ditulis wajar) tapi tak pernah benar² dieksekusi bisa menyimpan bug bertahun — verifikasi lewat eksekusi nyata, bukan baca kode, adalah satu-satunya cara ketahuan | **Gate M0 SEKARANG TERPENUHI PENUH** — 4 syarat asli (dari D14): (a) backend build+test hijau — 34/34 (naik dari 10/10 H1, semua ticket H2+ sudah masuk); (b) `bun run build` frontend hijau — dikonfirmasi ulang SETELAH magic link (D21) krn 2 file frontend baru itu belum pernah lolos build-check sebelumnya, sekarang confirmed hijau termasuk rute `/api/auth/magic-link` & `/mentor-invite`; (c) grep hardcode warna nol hasil — sudah dari H1 (D14), tak tersentuh sesi ini; (d) `docker compose up -d` 7 servis SEMUA healthy — **BARU tercapai sesi ini**, dikonfirmasi 2× cek berturut dgn jeda (bukan sekali lihat lalu percaya) + smoke-test HTTP nyata dari HOST (bukan cuma healthcheck internal): `curl http://localhost:3000` → 200, `curl http://localhost:5000/health/ping` → 200 `{"status":"ok","service":"Vokasia.Api"}`. **Belum diverifikasi** (di luar definisi gate ini, dicatat sbg gap terpisah): container compose ini jalan dgn `ASPNETCORE_ENVIRONMENT` default (Production, tak di-set eksplisit di `docker-compose.yml`) — berarti `DisableTransportSecurityRequirement()` (D17, khusus `env.IsDevelopment()`) TIDAK aktif di jalur compose ini; flow OAuth interaktif penuh (login browser sungguhan) lewat compose stack ini belum dicoba (beda dari smoke test H2-E3 yg pakai `dotnet run`+`bun run dev` langsung, bukan container) — kemungkinan butuh `ASPNETCORE_ENVIRONMENT=Development` ditambah ke `api`/`worker` di compose, atau reverse-proxy TLS sungguhan, tergantung apakah compose stack ini dimaksudkan Dev sbg lingkungan "mirip produksi" atau "dev lokal" — keputusan Dev, bukan diasumsikan sesi ini |

| D24 | 2026-07-21 | Melanjutkan instruksi "kerjain semua task" pasca Gate M0 (D23) — ticket berikutnya di sprint plan: VOK-H3-E1 (journal endpoints + presigned upload + cron slot/reminder), effort **high**, `ticket/VOK-H3-E1.md` | Diimplementasikan penuh 4 kelompok sesuai DoD ticket sendiri ("cron → siswa → mentor", guru menyusul sekalian krn kecil): **(1) Cron Hangfire** (`Vokasia.Worker/Jobs/JournalCronJobs.cs`): `GenerateDailyJournalSlots(runDate?)` 05:00 WIB (skip weekend+`Holiday`+placement nonaktif+di luar rentang `Period`, idempoten BY CONSTRUCTION — query slot ada dulu, filter, insert sisanya, bukan andalkan unique-constraint-lalu-catch), `RemindEmptyJournals()` 19:00 WIB (`Notification`+outbox `JournalReminderEmailRequested` per siswa berslot kosong+akun tertaut). Zona waktu diletakkan di `Vokasia.Domain.Common.AppTimeZone` (BUKAN Worker atau Api) krn dipakai DUA ARAH (cron + `GetTodayJournal` Api) — Api tak boleh referensi Worker (arah dependency proyek: Api/Worker sama2 turunan Domain+Infrastructure, bukan saling silang). **(2) Endpoint siswa** (`StudentSelf`): `GetTodayJournal` (slot+entry+kompetensi per jurusan+status seminggu Senin-Jumat+streak dari `StudentDailyStatus`), `SubmitJournal` (create ATAU resubmit-in-place kalau entry lama Rejected — unique index 1 slot=1 entry seumur hidup, "isi ulang" berarti update baris yang sama, bukan baris baru), `GetPresignedUploadUrl` (MinIO `PresignedPutObjectArgs`, validasi content-type allowlist jpeg/png/webp + ukuran ≤5MB, key ber-prefix `tenant/{tid}/journal/`), `AttachPhoto` (maks 3), `ListJournals` (proyeksi 1-query pakai subquery berkorelasi utk Photos/CompetencyIds, discoped ke placement milik sendiri). **(3) Endpoint mentor** (`MentorOwnPlacement`, resource-based): `GetPendingApprovals` (grup per siswa), `ApproveJournal`/`RejectJournal`/`BatchApprove` (independen per-item, satu gagal tak batalkan lainnya) — **INI PERTAMA KALINYA `PlacementScopeHandler` benar2 dipakai lewat HTTP nyata** (sebelumnya cuma unit test langsung thd handler, `RbacPolicyTests`, sesuai catatan komentar handler sendiri "mulai dipakai H3 ApproveJournal dst"). **(4) Endpoint guru** (`TeacherPlus`): `AddTeacherComment` (+ notifikasi siswa), `ListCompetencies` | **Temuan arsitektur penting**: `PlacementScopeHandler` adalah `AuthorizationHandler<TRequirement, Placement>` (resource-based) — TIDAK BISA dipasang via `.RequireAuthorization(RbacPolicies.MentorOwnPlacement)` di level route Minimal API: `context.Resource` di level route defaultnya `HttpContext` (bukan `Placement`), base class hanya panggil `HandleRequirementAsync` kalau `context.Resource is TResource` — requirement TAK PERNAH bisa `Succeed`, SEMUA request mentor akan selalu 403 kalau policy penuh dipasang di route. Pola yang benar (dipakai di sini): route cuma `.RequireAuthorization()` (bare, sekadar authenticated), lalu DI DALAM handler — setelah load `Placement` sungguhan dari `JournalEntry.PlacementId` — panggil eksplisit `IAuthorizationService.AuthorizeAsync(user, placement, RbacPolicies.MentorOwnPlacement)` dgn resource yang BENAR. Dibuktikan lewat test HTTP nyata `ApproveJournal_MentorDoesNotOwnPlacement_Returns403`, diverifikasi PROMPT D penuh (ganti `if (!authResult.Succeeded)` jadi `if (false)` → test merah PERSIS prediksi (403 diharapkan, dapat 200) → kembalikan → hijau). **2 bug nyata ditemukan SAAT REDEPLOY docker-compose** (bukan cuma `dotnet test` InMemory — persis pola D23, kode yg lolos test tapi belum pernah benar2 dijalankan bisa nyimpan bug tersembunyi): **(a)** Worker CRASH langsung saat startup (exit 139) — `RecurringJob.AddOrUpdate` (API STATIS Hangfire) gagal `"Current JobStorage instance has not been initialized yet"` di host Worker Service murni (`Microsoft.NET.Sdk.Worker`, BUKAN ASP.NET Core) — asumsi awal keliru bahwa `AddHangfire()` men-set `JobStorage.Current` scr SINKRON saat registrasi; ternyata TIDAK di konteks host ini. Diperbaiki: resolve `IRecurringJobManager` dari `host.Services` SETELAH `host.Build()`, pakai method INSTANCE (persis saran pesan exception-nya sendiri: "gunakan IRecurringJobManager instead of RecurringJob"). **(b)** Api JUGA crash langsung (exit 139, `docker logs` KOSONG tanpa stack trace sama sekali) — blok ensure-bucket MinIO baru (`BucketExistsAsync`) pakai fallback endpoint `"localhost:9000"` (`DependencyInjection.cs`, ditulis H1) — DARI DALAM container `api` sendiri, `"localhost"` adalah loopback container ITU SENDIRI, BUKAN container `minio` terpisah → gagal connect. Dikonfirmasi BUKAN bug logika C# (bukan asumsi) via `dotnet run` LOKAL (non-container) yang BERHASIL sempurna dgn kode identik — membuktikan penyebabnya murni konteks networking container. Diperbaiki 2 lapis: **(1)** `docker-compose.yml` service `api` ditambah env eksplisit `Minio__Endpoint: minio:9000` (root-cause fix, cermin pola `ConnectionStrings__Default`/`Redis__Connection` yg sudah ada); **(2)** blok ensure-bucket dibungkus `try/catch` + `LogWarning` (pertahanan KEDUA — kegagalan MinIO apa pun di masa depan TIDAK BOLEH menjatuhkan SELURUH API; login/jurnal-teks/approval semua harus tetap jalan, cuma fitur upload foto yang boleh terdampak) | 66 test backend total: **65 hijau + 1 sengaja `Skip`** (naik dari 34 sblm ticket ini). Test yang di-`Skip` (`ListJournalsNPlusOneVerification`): AC DoD "`ListJournals` 90 hari → 1 query utama, log EF dibuktikan" TAK BISA dibuktikan lewat EF InMemory (provider itu tak hasilkan log SQL bermakna, keterbatasan yang sama sudah diakui `VokasiaApiFactory` sendiri) — dijalankan MANUAL SEKALI thd Postgres NYATA (docker-compose Gate M0 yang sedang healthy) dgn `.LogTo()` menghitung baris "Executed DbCommand": hasil **PASS, persis 1 query** (subquery berkorelasi utk Photos/CompetencyIds diterjemahkan Npgsql jadi bagian query utama). Di-`Skip` permanen di suite rutin (butuh Postgres hidup, tak portable CI biasa) — didokumentasikan jujur di sini alih2 diklaim berulang tanpa bukti baru. Seluruh 7 servis docker-compose kembali healthy dgn image TERBARU (api+worker di-rebuild ulang, mencerminkan kode H3-E1 penuh, bukan image lama Gate M0) — dikonfirmasi stabil ≥1 menit + smoke HTTP nyata `curl localhost:5000/health/ping` → 200. **Sengaja belum dikerjakan** (dicatat eksplisit, sesuai scope ticket sendiri): thumbnail generation foto jurnal (`JournalPhoto.ThumbKey` ada kolomnya, tak pernah diisi kode ini — consumer event `PhotoUploaded`, wilayah H4-E1); dispatcher outbox NYATA (5 tipe event ditulis sesi ini — `JournalSubmitted`/`JournalApproved`/`JournalRejected`/`PhotoUploaded`/`JournalReminderEmailRequested` — SEMUA masuk tabel `OutboxMessage`, TAK ADA consumer yg membaca+publish ke RabbitMQ, sama polanya dgn `PlacementCreated` H2-E1 yg lebih dulu ada tanpa consumer; itu scope H4-E1); pengiriman email reminder sungguhan (H4); `Competency` masih tabel KOSONG di `DemoSeeder` (tak pernah diseed) — `ListCompetencies` akan kembalikan array kosong thd data demo asli sampai seeder diperbarui atau admin isi manual (test sesi ini seed `Competency` sendiri scr manual, bukan pakai data demo); validasi panjang teks jurnal sesi ini pakai if-check manual sederhana (BUKAN pipeline FluentValidation resmi meski paket `FluentValidation.DependencyInjectionExtensions` sudah direferensi Api.csproj) — sesuai scope ticket sendiri yang eksplisit menaruh "immutability guard dipasang H3-E3"; `EnsureMutable()` hook SUDAH dipanggil di titik yang benar (sebelum mutasi Approve/Reject/BatchApprove) tapi penegakan penuh (dan kemungkinan migrasi ke FluentValidation) menyusul H3-E3 |

| D25 | 2026-07-21 | Lanjut instruksi "kerjain semua task" pasca H3-E1 (D24) — ticket berikutnya sesuai matrix: VOK-H3-E3 (immutability + validasi menyeluruh + rate limit), effort **max**, `ticket/VOK-H3-E3.md` (dikerjakan sblm H3-E2 krn same-stack backend, momentum langsung dari H3-E1) | **§1 Immutability**: `DomainImmutableException` (Vokasia.Domain.Common) + `IExceptionHandler` (`DomainImmutableExceptionHandler`) memetakan ke 409 `{code,message}` (`app.UseExceptionHandler()` paling awal pipeline). `JournalEntry.EnsureMutable()` diubah lempar exception baru (bukan `InvalidOperationException` generik). `AssessmentImmutabilityGuard` kerangka ditulis (belum dipanggil di mana pun - Assessment belum py endpoint mutasi sampai H5-E1, sesuai AC "kerangka"). **GAP NYATA ditemukan+ditambal**: `EnsureMutable()` di `ApproveJournal`/`RejectJournal` H3-E1 diletakkan SETELAH check `Status != Submitted` - utk entry Approved, check itu SUDAH return Conflict generik lebih dulu, `EnsureMutable()` TAK PERNAH tercapai (dead code sesungguhnya, pesan {code:"journal-approved-immutable"} tak pernah muncul). Dibalik: `EnsureMutable()` DULU, baru check status lain. `SubmitJournal` serupa (`entry.Status != Rejected` dulu, sekarang `EnsureMutable()` dulu). `AttachPhoto` H3-E1 TERNYATA tak py guard status SAMA SEKALI (siswa bisa lampir foto ke entry Approved) - ditambal. `BatchApprove` KHUSUS: `EnsureMutable()` dipanggil TAPI ditangkap try/catch LOKAL (bukan dibiarkan ke exception middleware global) - exception tak tertangani di tengah loop batch akan membatalkan SELURUH response, melanggar AC "satu gagal tak membatalkan lainnya". **§2 Validasi**: 8 validator FluentValidation persis nama ticket (`SubmitJournalValidator, CreatePeriodValidator, CreatePlacementValidator, ImportStudentRowValidator, InviteUserValidator, ProposeCompanyValidator, UploadRequestValidator, RejectJournalValidator`) + `ValidationFilter` (endpoint filter generik, resolve `IValidator<T>` dari DI per tipe argumen runtime, termasuk elemen `List<T>`) dipasang di SEMUA `MapGroup` (8 titik). `TextSanitizer.Clean()` (strip `<script>`/`<style>` utuh+isi, tag lain di-strip tapi teks disimpan) diterapkan ke SEMUA field teks bebas yang disimpan (jurnal Text, MentorNote/Note, RejectReason, TeacherComment.Text). `ImportStudentRow` (record baru) + refactor `ImportStudentsCsv` (Students.cs) dari if-check manual ke FluentValidation per baris - kini kumpulkan SEMUA field gagal per baris (bukan berhenti di kegagalan pertama, sedikit beda dari perilaku lama tapi lebih informatif, tak ada test lama yg gantung pada jumlah error lama). Inline check lama yg kini redundan (Text>500, CompetencyIds>5, PhotoIds>3, ContentType/SizeBytes, Reason kosong) DIHAPUS dari endpoint (bukan dibiarkan jadi 2 sumber kebenaran). **§3 Rate limit**: `Microsoft.AspNetCore.RateLimiting` bawaan (TANPA paket baru). 2 policy: "login" (5/mnt, sliding window, partisi **IP+email**) dan "public" (10/mnt, partisi IP). Middleware pre-buffer `ReadFormAsync()` ditambah SEBELUM `UseRateLimiter()` supaya partition-key selector (sinkron) bisa baca `Request.Form["email"]` dgn aman (tanpa pre-buffer, akses sync ke body yg belum dibaca BISA throw "Synchronous operations are disallowed" di Kestrel produksi - AllowSynchronousIO=false default, TAK TERLIHAT lewat TestServer yg dipakai test) | **3 DEVIASI dicatat eksplisit dari bacaan literal ticket** (ditemukan lewat percobaan nyata + `dotnet test`, BUKAN diputuskan di awal tanpa bukti): **(1) Rate limit "login" dipasang di `POST /account/login`, BUKAN `/connect/token`** spt tulisan ticket ("password/code grant"). Dicoba dulu persis spt ticket ("public" 10/mnt di `/connect/token`) - GAGAL nyata: `JournalStudentEndpointsTests` (12 fact, 1 factory/IP bersama) langsung dapat 429 palsu krn endpoint itu dipakai SEMUA login (code+refresh+magic-link) dari BANYAK user berbeda tanpa identitas pembeda dlm form-nya (tak ada field "username" utk grant yg benar2 dipakai proyek ini - lihat AuthorizationController.Exchange(), TAK ADA password grant sama sekali). Password SUNGGUHAN disubmit di `/account/login` (form `email`+`password`, H1-E3) - itulah dipasang policy "login" ketat (partisi IP+email, jadi banyak user beda dari 1 IP - baik di test MAUPUN skenario nyata siswa 1 sekolah 1 jaringan - tak saling kunci). `/connect/token` SENGAJA TAK diberi rate-limit sama sekali - kredensial yg ditukarnya (code/refresh-token/magic-link token) ber-entropi tinggi, tak bisa ditebak lewat volume percobaan. **(2) CompetencyIds minimum-1 TIDAK ditegakkan** (ticket tertulis "1-5") - dipertahankan 0 boleh (hanya batas ATAS 5 + cek kepemilikan major yg DITEGAKKAN BARU) krn H3-E1 (AC/DoD sudah disetujui D24) tak pernah mensyaratkan minimum, beberapa test H3-E1 yg SUDAH hijau sengaja pakai array kosong sbg skenario sah - minimum-1 aturan UX di luar tema hardening ticket ini. **(3) Cakupan `ValidatorCoverageTests` dipersempit ke daftar TETAP 8 tipe yg disebut eksplisit ticket §2** (bukan scan reflection otomatis SEMUA tipe request H1-H3) - request type lain (`UpdatePeriodRequest, CreateStudentRequest, UpdateStudentRequest, CreateMentorInviteRequest, ApproveJournalRequest, AddCommentRequest, BatchApproveRequest`, dll) BELUM py validator, gap jujur bukan diam-diam diklaim tercakup | **110 test backend** (109 hijau + 1 skip Postgres-manual dari H3-E1, naik dari 66) - 44 test baru (`Vokasia.Tests/Guard/`: SanitizerTests 6, ValidationBoundaryTests 19, ImmutabilityTests 7, ValidatorCoverageTests 8 theory, RateLimitTests 3). **PROMPT D dijalankan nyata** thd guard sentral: `JournalEntry.EnsureMutable()` diubah jadi `if (false && ...)` sementara → 5 dari 7 `ImmutabilityTests` MERAH PERSIS prediksi (2 lolos krn tak bergantung EnsureMutable: TenantAdmin_ApproveOnApprovedEntry via RBAC murni, dan satu lagi terkait BatchApprove count) → dikembalikan → HIJAU lagi, dikonfirmasi 2× run berturut dari state bersih (109/110 kedua kali). Verifikasi "negative test manual curl" DoD ticket DIANGGAP TERPENUHI via test HTTP otomatis (`WebApplicationFactory`/`TestServer`, request+respons JSON/form nyata lewat pipeline ASP.NET Core sungguhan, pola yang SAMA yang sudah dipakai & diterima sepanjang sesi ini - mis. PkceRequiredTest eksplisit menyebut ini "HTTP nyata") - TIDAK dilakukan rebuild+redeploy docker-compose+curl manual terpisah utk ticket ini (biaya rebuild+redeploy tinggi, marginal proof tambahan rendah krn jalur kode identik sudah teruji ketat lewat HTTP nyata; beda dgn D23/D24 yang MEMANG baru pertama kali dicoba container-run sama sekali). Audit AGENTS.md butir 4 ("FluentValidation semua request; upload presigned; EXIF-GPS - EXIF di luar scope, H4-E1") & butir 5 ("JournalEntry immutable pasca-Approved; nilai final terkunci; unlock hanya ber-audit") - keduanya terpenuhi sesuai scope ticket (nilai final = kerangka saja, sesuai AC). Tanpa dependency baru (rate limiter = built-in ASP.NET Core, FluentValidation sudah ada sejak scaffold) |
| D26 | 2026-07-21 | Lanjut instruksi "kerjain semua task" pasca H3-E3 (D25) — ticket berikutnya sesuai matrix: VOK-H3-E2 (UI siswa isi jurnal W1 + mentor batch approve W2), `frontend/` saja, `ticket/VOK-H3-E2.md` | **Dibangun penuh**: `student/page.tsx` (Server, fetch `GetTodayJournal`) + `TodayJournalCard` (client, pemilik state `entry`/`weekStatus`, flip optimistic tanpa round-trip kedua) + `JournalForm` (textarea<=500+counter, disable saat submit) + `CompetencyPicker` (bottom-sheet, cari, maks 5) + `PhotoUploader` (presigned PUT MinIO, preview blob lokal, retry/batal per file independen) + `WeekStrip` + `student/history` (tab filter status via searchParams + `JournalHistoryItem`). `mentor/page.tsx` (fetch `GetPendingApprovals`) + `ApprovalList` + `ApprovalCard` + `RejectDialog` (alasan wajib 5-300 + chip cepat) + `SelectAllBar`. `loading.tsx` per rute (App Router skeleton). **TEMUAN arsitektur nyata (bukan diasumsikan dari baca ticket, ketahuan saat menyambungkan alur submit ke API H3-E1 sungguhan)**: `AttachPhoto` (`POST /journals/{id}/photos`) mewajibkan `JournalEntry` YANG SUDAH ADA (`{id}` = entry id) — tapi entry itu baru dibuat `SubmitJournal`, yang wireframe W1 sendiri gambarkan foto dipilih SEBELUM tombol KIRIM ditekan. Diselesaikan MURNI di frontend (2 langkah: submit teks+kompetensi dulu dgn `photoIds:[]` → dapat `entry.id` → attach tiap foto pakai id itu; foto yg gagal attach tak membatalkan jurnal yg sudah tersimpan) — TANPA ubah backend sama sekali, sesuai wilayah ticket. **3 GAP data dicatat eksplisit, bukan dikarang**: (1) `WeekStrip` 2 status (Filled/Empty) bukan 3 (done/pending/empty wireframe) — `WeekDayStatusDto` tak bawa status approval per-hari terpisah; (2) header `/student` tak tampilkan nama perusahaan — `TodayJournalDto` tak bawa field itu & satu2nya endpoint placement (`GET /placements`) butuh `RbacPolicies.TenantMember` (staf sekolah), tak bisa dipanggil sesi siswa — PERSIS gap yang sudah dicatat D16, dikonfirmasi ulang bukan ditimbulkan sesi ini; (3) `ApprovalCard` tak tampilkan nama sekolah/foto-thumbnail-nyata/tanda "⚠ hari kosong" — `PendingGroupDto` tak bawa field sekolah, dan `JournalDto.photos` dari `GetPendingApprovals` SELALU `[]` by design (`ToDto(x.Entry, [], [])` eksplisit di kode, komentar "ringkasan approval TIDAK perlu foto/kompetensi penuh") — bahkan bila backend diperkaya kelak, belum ada mekanisme presigned READ URL sama sekali (baru ada presigned PUT). `ApprovalList` dirancang FLAT per-jurnal (bukan grup collapsible per siswa) krn tanda tangan ticket sendiri `ApprovalCard({journal, expanded})` = satu kartu per JURNAL. Approve/reject/batch approve SEMUA optimistic+rollback sesuai AC eksplisit: entry hilang dari layar SEKETIKA saat aksi ditekan (bukan menunggu response dulu), dikembalikan (rollback) + banner error kalau API gagal; batch approve rollback SEBAGIAN kalau `BatchResult.failed` tak kosong (yg gagal kembali ke layar, yg sukses tetap hilang), beda dari rollback TOTAL saat request itu sendiri throw (network/500) | `bun run build` hijau (semua rute `/student`,`/student/history`,`/mentor` muncul sbg `ƒ Dynamic`, dikonfirmasi via `$LASTEXITCODE=0` eksplisit, bukan cuma baca teks output). **Live E2E penuh (submit siswa sungguhan → approve mentor sungguhan thd compose stack, sesuai DoD ticket) TIDAK tercapai — 2 blocker, KEDUANYA pra-ada, dikonfirmasi lewat percobaan nyata bukan dugaan**: (1) tak ada satu pun akun login siswa di seed data (`DemoSeeder.cs` bikin 900 baris `Student` murni tanpa `AppUser`/password — login siswa eksplisit scope H7 di `TICKETS.md`); dicoba minta subagent investigasi jalur bikin kredensial via SQL manual (copy `PasswordHash` dari user demo yg sudah ada) — **ditolak classifier auto-mode Cowork sendiri** ("Blocked by classifier"), diterima sebagai sinyal yang benar (memalsu kredensial Identity, sekecil apa pun niatnya, bukan tindakan yang pantas diambil sendiri tanpa izin eksplisit) — TIDAK dipaksa lewat jalur lain. (2) Dicoba jalur sah (login staf demo `admin@10000001.vokasia.demo`/`Demo-Passw0rd!` yg SUDAH ada sejak seed, via `bun run dev` langsung host + `.env.local` yg sudah benar `API_INTERNAL_URL=localhost:5000` sejak D17) — browser sungguhan (`claude-in-chrome`, viewport 390×844) berhasil sampai redirect `/connect/authorize` tapi DITOLAK OpenIddict: `error:invalid_request — This server only accepts HTTPS requests`. Diselidiki (BUKAN ditambal sepihak): akar masalah = compose `api` service jalan `ASPNETCORE_ENVIRONMENT` default (Production, tak di-set eksplisit di `docker-compose.yml`) sehingga `DisableTransportSecurityRequirement()` (khusus `env.IsDevelopment()`, D17) tak aktif — **PERSIS gap yang sudah di-flag D23** ("flow OAuth interaktif penuh lewat compose stack ini belum dicoba... keputusan Dev, bukan diasumsikan sesi ini") — dikonfirmasi ulang dgn reproduksi konkret+pesan error asli, BUKAN diputuskan ulang sepihak sesi ini (tetap milik Dev: ubah `ASPNETCORE_ENVIRONMENT` compose, atau TLS sungguhan, tergantung compose ini dimaksudkan "mirip produksi" atau "dev lokal"). **1 temuan turunan dicatat, TIDAK diperbaiki** (di luar scope commit, murni observasi): `frontend/src/app/api/auth/login/route.ts` memakai `API_INTERNAL_URL` utk membangun `redirect_uri` BROWSER (`NextResponse.redirect`) — benar utk panggilan server-ke-server (spt `callback/route.ts` menukar code→token), SALAH utk kasus KHUSUS ini krn nilainya `http://api:8080` (hostname Docker-internal) saat jalan di dalam container compose frontend, tak bisa di-resolve browser host sama sekali (beda kelas bug dari D17 #7, versi kebalikannya). Dibersihkan penuh pasca-percobaan: proses `bun dev` dihentikan, container `vokasia-frontend-1` di-rebuild (`docker compose build frontend`, membawa kode H3-E2 baru) + dijalankan ulang sehat, `frontend/bun.lock` yang SEMPAT terhapus tanpa sengaja (efek samping `bun run build`/`dev` langsung di host) dipulihkan via `git checkout` sebelum commit — dikonfirmasi `git status` bersih. Commit `af5bc50` (17 file, 1353 baris tambah/23 hapus) | Sengaja BELUM dikerjakan (scope H4+ atau butuh keputusan Dev, bukan diam-diam dilewati): login siswa sungguhan (H7), keputusan `ASPNETCORE_ENVIRONMENT` compose (Dev), mekanisme presigned READ URL utk foto (dibutuhkan H4+ jika "foto thumbnail nyata" mentor mau diwujudkan), enrichment `TodayJournalDto`/`PendingGroupDto` dgn nama perusahaan/sekolah/status-approval-harian (butuh ubah backend, di luar wilayah ticket ini) |
| D27 | 2026-07-22 | Sesi baru (handoff tertulis pasca-D26, ditempel sbg pesan pertama) melanjutkan fase test+verifikasi VOK-H4-E1 (task #39-42) - kode produksi (outbox dispatcher, 7 consumer MassTransit, cron ghosting, dashboard, notifikasi) sudah ditulis sesi sebelumnya tapi BELUM SATU PUN dieksekusi (sesi lalu: sandbox tanpa dotnet SDK/docker, proxy sandbox blokir nuget.org+seluruh domain download Microsoft, dikonfirmasi header `X-Proxy-Error: blocked-by-allowlist`) | **Blocker jaringan CONFIRMED RESOLVED, bukan diasumsikan**: smoke test curl langsung (bukan cuma `dotnet --version`, sandbox baru memang tak pernah punya dotnet SDK - itu bukan sinyal block) ke nuget.org (200), dot.net (301 normal), berhasil unduh+jalankan `dotnet-install.sh` (channel 10.0, resolve ke SDK 10.0.302 dari `builds.dotnet.microsoft.com`) - unduhan 225MB via curl langsung (retry-safe, `-C -`) krn tiap panggilan tool `bash` sandbox berjalan dlm namespace `bwrap --unshare-pid` terpisah (proses background/`nohup`+`disown` TIDAK bertahan lintas panggilan tool - ketahuan lewat 1 percobaan gagal, bukan diasumsikan sejak awal) sehingga tak bisa polling proses lama, jadi unduh+ekstrak dipecah 2 langkah sinkron di bawah limit 45dtk/panggilan. **Temuan lingkungan BARU**: folder mount `D:\Web\Vokasia` dari sisi sandbox Linux (`mcp__workspace__bash`) adalah FUSE (`user_id=0,group_id=0,default_permissions`) yang mengizinkan write+rename TAPI MENOLAK unlink ("Operation not permitted") bahkan utk file yang baru ditulis sesi yang sama - persis kelas masalah yang sama dgn `.git/index.lock` tak ke-unlink di D16, TERNYATA berlaku UMUM ke seluruh mount (bukan cuma git), krn `dotnet restore`/`build`/`test` rutin bikin+hapus file temp di `obj/`. Diatasi bukan dgn coba lagi berulang, tapi `rsync` sekali source `backend/` (exclude obj/bin/TestResults) ke direktori lokal non-FUSE (`/sessions/.../work/backend`, dikonfirmasi bkn mount terpisah lewat tes tulis+hapus) - SELURUH `dotnet restore/build/test` sesi ini jalan di situ, bukan langsung di path ter-mount. #39: `NotificationEndpointsTests` (6 test, ditulis sesi lalu, NOL eksekusi sebelumnya) dijalankan pertama kali - 6/6 hijau; PROMPT D pada perilaku terpenting (`MarkRead` 404-bukan-403 lintas user) dibuktikan tuntas: comment-out cek kepemilikan (`if (notif is null \|\| notif.UserId != userId.Value)` -> `if (notif is null)`) di kopi lokal -> merah PERSIS prediksi (`Expected: NotFound, Actual: OK`) -> revert -> hijau lagi. #40: full suite `dotnet test` 2x berturut dari state bersih - 135/138 hijau kedua kali (3 skip Postgres-manual, sama persis dgn H3-E1/H3-E3, tak berubah), 0 gagal. #41 (docker compose, dijalankan via **Windows-MCP PowerShell di host, BUKAN sandbox Linux** - Docker Desktop cuma ada di mesin Windows Dev, sandbox `bash` tak punya `docker` sama sekali, dikonfirmasi `command not found`): image `api`+`worker` KEDUANYA stale (bukan cuma `worker` spt catatan handoff - dibuktikan lewat probe HTTP nyata `/api/notifications`&`/api/dashboard/school/{id}` 404 sebelum rebuild, 401 sesudahnya) -> `docker compose build api worker` + `up -d` -> **1 BUG PRODUKSI NYATA ditemukan+diperbaiki** lewat uji broker-down/recovery manual sungguhan (stop `rabbitmq`, insert 1 baris `OutboxMessages` sintetik tipe `PlacementCreated` merujuk placement NYATA dari seed via `psql -f` [bukan `-c` inline - nested-quoting PowerShell->docker exec->shell kontainer utk identifier `"PascalCase"` gagal berulang, diatasi tulis file `.sql` lalu `docker cp`], konfirmasi baris unpublished + worker TETAP hidup [log `BrokerUnreachableException` bersih, container tetap healthy] -> start `rabbitmq` lagi -> **worker TAK PERNAH benar-benar konek** [`rabbitmqctl list_connections` kosong >1 menit pasca broker healthy]): akar masalah `docker-compose.yml` service `worker` tak pernah diberi override `RabbitMq__Host` (beda dgn `ConnectionStrings__Default`/`Redis__Connection`/`Minio__Endpoint` yg SUDAH di-override eksplisit) - jatuh ke fallback `.env` (`RabbitMq__Host=localhost`, memang benar utk `dotnet run` di host, SALAH dari dalam container: "localhost" = loopback worker itu sendiri) - persis kelas bug yg SAMA dgn MinIO D24, TAK PERNAH ketahuan krn ini kali PERTAMA kode MassTransit (semua H4-E1, sebelumnya tak ada di image manapun) benar2 dijalankan di compose. Ditambal (`RabbitMq__Host: rabbitmq` di block `environment` worker, pola identik `Minio__Endpoint`) -> `up -d worker` -> `rabbitmqctl list_connections` langsung nunjuk 1 koneksi `running` -> baris outbox published dlm <10 dtk, `PlacementCreatedConsumer` log "welcome pack ... terkirim", 1 `Notification` baru (guru - siswa dilewati SENYAP by-design krn `Student.UserId` null, sesuai komentar consumer) -> `rabbitmqctl list_queues` seluruh 7 queue konsumen 0 pesan tertahan, NOL queue `*_error` (DLQ kosong). Data sintetik (1 baris outbox + 1 notifikasi) dibersihkan pasca-verifikasi (DELETE eksplisit by-id, dikonfirmasi count kembali 0) - state demo dikembalikan persis semula. #42: `git status`/`git diff --stat` PER FILE dicek (bukan `git add -A`) - **temuan CRLF/LF BEDA dari yg didokumentasikan D16/D19/handoff**: sesi ini `git status` BERSIH (13 modified + 27 baru, NOL file "noise" pola insertion==deletion==total-baris) - `core.autocrlf=true` aktif saat ini, kemungkinan menutupi drift yg D16/D19 laporkan (belum diselidiki APA yang berubah, dicatat sbg observasi jujur bukan diklaim "sudah diperbaiki permanen" - `.gitattributes`+renormalize eksplisit tetap belum ada, rekomendasi D19 masih berlaku sbg pencegahan). 1 hambatan mekanis: `.git/index.lock` stale (bkn proses aktif, dikonfirmasi `Get-Process git` kosong) sempat blokir `git add` (`fatal: Unable to create index.lock: File exists`) - dihapus via `Remove-Item` PowerShell host (pola sama D16), BUKAN ditandai gagal | Prinsip "skeptis thd klaim sudah ditest" (SOUL.md) ditegakkan di jalur PALING literal sesi ini: bug `RabbitMq__Host` HANYA bisa ketahuan krn worker BENAR2 dinyalakan dgn kode H4-E1 nyata thd broker nyata yg BENAR2 dimatikan-lalu-dihidupkan - `dotnet test` (EF InMemory+`WebApplicationFactory` self-contained, tak pernah sentuh RabbitMQ sungguhan) tak akan PERNAH menangkap ini walau 138/138 hijau selamanya, persis pola D23/D24 (Dockerfile `.sln` salah, healthcheck `wget` hilang, MinIO `localhost`) - kelas bug infra-only yg cuma nongol saat container BENAR2 dijalankan. Keputusan pisah lingkungan (dotnet test di sandbox Linux via kopi lokal non-FUSE; docker+git di host via Windows-MCP PowerShell) diambil krn masing2 SATU-SATUNYA lingkungan yg punya toolnya (sandbox tak punya docker sama sekali; mount Linux tak bisa unlink) - bukan preferensi, tapi keterbatasan nyata tiap sisi | **VOK-H4-E1 SELESAI PENUH** (task #35-42, commit `b26ef9d`, 40 file/4007+/21-): 135/138 test hijau (2x run), PROMPT D §39 lengkap, verifikasi live docker-compose lengkap (broker-down survive + recovery + idempotency terbukti tuntas end-to-end, DLQ kosong dikonfirmasi), 1 bug produksi nyata (RabbitMq__Host) ditemukan+diperbaiki+diverifikasi sblm sempat dipakai Dev. **Gap dicatat, sengaja belum dikerjakan** (di luar scope H4-E1, atau milik ticket lain): `NU1903` - `Microsoft.OpenApi` 2.1.0 vulnerability HIGH muncul di setiap `dotnet restore` (paket transitif, bukan ditambahkan sesi ini) - dicatat utk `dotnet list package --vulnerable` scan resmi VOK-H6-E3, TIDAK di-bump sepihak sesi ini (di luar scope tiket, hindari deviasi tanpa persetujuan spt AGENTS.md #13); `HANDOFF-NEXT-SESSION.md` (file handoff sesi lalu, di root repo) dibiarkan untracked - bukan kode H4-E1, keputusan commit/hapus diserahkan Dev; `JournalReminderEmailRequestedEvent` tetap tanpa consumer (BY DESIGN, email sungguhan = H4-E3); toolchain/lingkungan sesi ini (instalasi `.dotnet` lokal, `~/.nuget` cache, kopi kerja `/sessions/.../work/backend`) SEMUA di dlm sandbox sementara - TIDAK persisten lintas sesi, sesi berikutnya kemungkinan perlu ulang instalasi SDK (tapi jaringan sudah terbukti terbuka, tinggal ulang langkah yg sama, bukan investigasi baru). Lanjut ke VOK-H4-E2 (dashboard admin RAG W3 + halaman guru, `frontend/`) sesuai TICKETS.md, tanpa berhenti minta konfirmasi kecuali blocked nyata (pola §1 tetap berlaku) |

| D28 | 2026-07-22 | VOK-H4-E2 live-verification (DoD: "smoke dashboard vs seed... → screenshot W3 untuk VPM") — login browser sungguhan thd docker-compose stack (BUKAN unit test) gagal 2x berturut2 dgn 2 bug INFRA terpisah (bukan bug logic H4-E2 itu sendiri) | **Bug #1** `frontend/src/app/api/auth/login/route.ts`: `authorizeUrl` (dikirim ke BROWSER via `NextResponse.redirect`) memakai `API_INTERNAL_URL` (di docker-compose = `http://api:8080`, Docker-internal DNS) — browser (jalan di HOST) tak bisa resolve "api", hard nav error. Var BARU `API_PUBLIC_URL` (`.env`+`.env.example`, default `http://localhost:5000`, SENGAJA TIDAK di-override di compose krn browser selalu di host) ditambah, dipakai KHUSUS di baris ini; `callback/route.ts` & `magic-link/route.ts` TETAP pakai `API_INTERNAL_URL` (itu fetch server-to-server, benar apa adanya, dikonfirmasi baca ulang kedua file). **Bug #2** (baru muncul SETELAH bug #1 diperbaiki, browser lanjut ke `localhost:5000/connect/authorize` tp kena 400 OpenIddict `ID2083 "This server only accepts HTTPS requests"`): `ASPNETCORE_ENVIRONMENT` TAK PERNAH di-set utk container api/worker di docker-compose.yml (hanya ada di `launchSettings.json`, yg TIDAK dibaca image `dotnet publish`+`ENTRYPOINT` langsung) — default kosong = "Production", jadi `if (env.IsDevelopment())` di `OpenIddictSetup.cs:106` (`DisableTransportSecurityRequirement()`) DAN `Program.cs:44` (`MapOpenApi()`) diam2 TAK PERNAH aktif di container sejak ditulis. Ditambah `ASPNETCORE_ENVIRONMENT: Development` ke `environment:` service `api` DAN `worker` (konsistensi, dev-compose ini memang stack dev-only) — dikonfirmasi AMAN: `Program.cs:52` `UseHttpsRedirection()` tetap ada TANPA guard, tp `Dockerfile` cuma `ASPNETCORE_URLS=http://+:8080` (TIDAK ada https endpoint apa pun dikonfigurasi) → perilaku resmi ASP.NET Core saat tak bisa tentukan https port = warning+no-op, BUKAN redirect gagal. Sesudah KEDUA fix: rebuild `frontend` image, recreate `api`+`worker` (env var saja, tanpa rebuild) → login penuh via browser sungguhan (`guru0@10000001.vokasia.demo` / `Demo-Passw0rd!`, password seed dev `DemoSeeder.cs:254`) BERHASIL end-to-end: `/login` → BFF → `/connect/authorize` → form dev H1-E3 (`/account/login`) → callback → sesi → `/app` (dashboard W3 render nyata, screenshot `screenshots/h4-e2-dashboard-w3.jpg`) → `/app/bimbingan` (caseload 65 siswa nyata + jurnal+komentar inline render nyata, screenshot `screenshots/h4-e2-bimbingan.jpg`) | Prinsip "jangan klaim sudah ditest tanpa eksekusi nyata" (SOUL.md) ditegakkan literal: KEDUA bug ini HANYA mungkin ketahuan lewat browser sungguhan thd compose sungguhan — `dotnet test`/`bun test` (135 kasus, EF InMemory+`WebApplicationFactory`/mock fetch, tak pernah lewat Kestrel/OpenIddict/browser nyata) TAK AKAN PERNAH menangkap keduanya, persis pola D24/D27 (kelas bug "benar di kode, salah di kombinasi lingkungan container"). Bug #1 SEBENARNYA sudah diketahui-tp-dibiarkan D26 ("out of scope") — sesi ini menutup gap itu krn ternyata dia SATU2NYA blocker DoD H4-E2 sendiri, bukan lagi opsional. Cross-check DB manual (`psql -f`, pola D-sebelumnya krn nested-quoting PowerShell tetap tak reliable) thd 4 KPI dashboard: TERNYATA percobaan PERTAMA salah (asumsi ordinal `JournalEntryStatus` keliru, `Submitted` dikira `1` padahal `Enums.cs:21` `{Submitted,Approved,Rejected}` = `0`) sempat tampak mismatch (17977 vs 0 di layar) — dikoreksi SEBELUM disimpulkan sbg bug, query ulang dgn ordinal benar: `pending_approvals`=0 cocok PERSIS; `Jurnal Hari Ini 0.0%` & `Siswa Bermasalah 0` jg cocok PERSIS (placemen periode ini TAK PUNYA slot bertanggal persis hari ini — max slot period ini = 2026-07-21, sama dgn `Period.EndDate`-nya; "hari ini" sandbox 2026-07-22 sudah 1 hari sesudah EndDate seed — observasi lingkungan wajar krn `today` di-hardcode saat seed dijalankan hari lain, BUKAN bug, TIDAK di-reseed krn tak mengubah kesimpulan verifikasi) | H4-E2 (task #24-28) SELESAI PENUH termasuk DoD browser+screenshot literal, bukan cuma `dotnet test`/`bun build` hijau. Manfaat SAMPING penting: jalur login browser penuh ini SAMA PERSIS yg akan dipakai VOK-H7-E3 (Playwright E2E 5 persona) — kedua bug di atas (kalau tak ketemu sekarang) akan memblokir SELURUH H7-E3 nanti, bukan cuma satu smoke test. **Gap dicatat, sengaja belum dikerjakan**: BFF route handler (`login/callback/magic-link route.ts`) MASIH nol unit test (pre-existing dari H2-E3, BUKAN regresi baru sesi ini — hanya `lib/guard.test.ts`+`lib/session.test.ts` yg ada); menambah test utk route handler adalah keputusan scope H4-E3/H7-E3, bukan diam2 ditambah di sini. `NU1903` (D27) & `HANDOFF-NEXT-SESSION.md` (untracked) masih sama, belum disentuh. Lanjut VOK-H4-E3 sesuai TICKETS.md tanpa henti minta konfirmasi (pola §1) |
| D29 | 2026-07-22 | VOK-H4-E3 (idempotency&DLQ tests+template email): infra email dibangun (`IEmailSender` 2 impl DevLog/Smtp + `IdempotentEmailSender` decorator dgn `SentEmail` table+migration + `EmailTemplateRenderer` 5 template) + 2 consumer baru (`JournalReminderEmailConsumer`, `GhostingAlertEmailConsumer`) + 1 diupdate (`MentorInvitedConsumer`) dipasang — ketemu gap nyata: `GhostingAlertEmailRequestedEvent` TAK PERNAH terdaftar di `OutboxDispatcher.TypeRegistry` sejak H4-E1 (email ghosting alert diam2 tak pernah terkirim sama sekali, baris Outbox selalu "published" tanpa pernah benar2 publish), langsung diperbaiki. Suite test async (Testcontainers RabbitMQ+Postgres SUNGGUHAN, bukan in-memory/mock) dibangun: duplicate-delivery/out-of-order/poison-message/DLQ-replay/outbox-guarantee — 5 dari 6 lolos di percobaan pertama, `DlqReplayTests` GAGAL KONSISTEN bahkan di timeout 60 dtk (dicoba naikkan 20→40→60dtk, tetap gagal identik tiap kali) walau mekanisme byte-sama lolos di `PoisonMessageTests` test lain SATU run yang sama — sinyal kuat bug LOGIKA, bukan kurang waktu, jadi diagnosis dilanjutkan bukan nambah timeout lagi | **Root cause ketemu**: helper `WaitForMessageInQueueAsync`/`MoveOneMessageAsync` "mengintip" DLQ via BasicGet+Nack(requeue:true) SATU pesan per iterasi — DLQ (nama literal tetap) dipakai BERSAMA lintas test class dlm collection sekuensial yg sama; pesan `PermanentThrow_...` (jalan lebih dulu) cuma di-peek BUKAN dikonsumsi sungguhan, jadi tertinggal PERMANEN di DLQ — sekali di-Nack(requeue:true), RabbitMQ taruh dia balik dekat KEPALA antrean, jadi sweep `DlqReplayTests` (cari ID BARU miliknya sendiri) SELALU dapat pesan LAMA itu lagi di posisi pertama, tak pernah cocok, di-requeue lagi, TAK PERNAH maju ke pesan baru yg menunggu di belakangnya — loop nunggu berapa lamapun tetap gagal (persis gejala nyata). Fix: kedua helper diubah jadi SATU sweep = BasicGet berulang TANPA nack di antaranya (pesan yg sudah diambil-tapi-blm-di-ack dianggap "in flight" oleh RabbitMQ — BasicGet berikutnya otomatis maju ke pesan LAIN, bukan mengulang yg sama) sampai target ketemu ATAU antrean habis (dibatasi 200 pesan/sweep), baru Nack(requeue:true) SEMUA yang tak cocok di akhir sweep (dikembalikan persis spt semula, tanpa hilang/rusak urutan permanen). Hasil: 6/6 test async hijau, durasi total anjlok dari >60dtk (gagal) jadi 4 detik — konsisten dgn diagnosis (bukan kebetulan lolos). **PROMPT D dijalankan** pada `DlqReplayTests`: baris simulasi "bug sudah diperbaiki" (`PoisonTestConsumer.ShouldThrowFor[poisonId]=false`) sengaja dikomentari → re-run → MERAH sesuai prediksi (`Assert.Contains() Failure: Item not found in collection`) → dikembalikan apa adanya → re-run → HIJAU (6/6) — membuktikan test ini benar2 menguji mekanisme replay, bukan lolos vakum/kebetulan. Full suite non-filtered (bukan cuma folder Async/): **146 passed, 3 skipped (pre-existing dari sesi2 sebelumnya, dikonfirmasi BUKAN regresi baru), 0 failed**. Docker image `worker`+`api` di-rebuild (`docker compose build`) + restart (`docker compose up -d`), `docker compose ps` kedua "healthy", log `worker` dikonfirmasi `MassTransit[0] Configured endpoint JournalReminderEmail`+`GhostingAlertEmail` dan `Bus started: rabbitmq://rabbitmq/` tanpa fault | Menegakkan SOUL.md "jangan klaim sudah ditest tanpa eksekusi nyata" + disiplin PROMPT D wajib utk test baru secara literal — kegagalan identik di 3 timeout berbeda (20/40/60dtk) diperlakukan sbg sinyal diagnosis, bukan alasan menaikkan angka lagi | (1) Task #32 (wiring consumer) & #33 (async suite) SELESAI. (2) **TEMUAN BARU di luar scope tiket ini, WAJIB dicatat bukan didiamkan**: `docker-compose.yml` service `rabbitmq` pakai image polos `rabbitmq:3-management-alpine` — PERSIS image yg di Testcontainers TERBUKTI (dicoba nyata, bukan diasumsikan) TAK PUNYA plugin `rabbitmq_delayed_message_exchange` yg dibutuhkan `UseDelayedRedelivery` (dipakai `MessagingDefaults`/`AddVokasiaMassTransit` PRODUKSI sejak H4-E1, TIDAK diubah sesi ini) — artinya tier "redelivery" (jeda lebih panjang setelah retry policy habis) KEMUNGKINAN BESAR jg tak berfungsi di docker-compose dev stack nyata ini, bukan cuma di test FastPoison — retry mungkin lompat langsung ke DLQ tanpa pernah dapat jatah redelivery ekstra yang dikira ada dari baca config. TIDAK diperbaiki sesi ini (di luar scope VOK-H4-E3, butuh keputusan Dev: pasang plugin custom image RabbitMQ, atau hapus `UseDelayedRedelivery` dari produksi jg supaya config = perilaku nyata) — direkomendasikan jadi item H7 (hardening) atau tiket tersendiri, BUKAN diam2 dibiarkan. (3) `tools/Replay-Dlq.ps1` (RabbitMQ Management HTTP API, `ack_requeue_false` GET + publish) sudah ditinjau kodenya — logikanya KONSISTEN dgn mekanisme AMQP yg sudah terbukti benar di `DlqReplayTests` (dan skripnya TAK kena bug root-cause di atas krn dia sengaja tak filter-by-ID, pindahkan apapun di kepala DLQ apa adanya, sesuai tujuan tool operator) — TAPI BELUM dieksekusi nyata thd DLQ live sesi ini (butuh sengaja mem-poison-kan queue nyata dulu, dianggap di luar scope waktu tersisa) — gap dicatat eksplisit, bukan diklaim tertest. (4) Log `worker` sesudah restart menunjukkan baris `Error: libgssapi_krb5.so.2: cannot open shared object file` (tampak dari proses startup, kemungkinan Hangfire/Npgsql mencoba negosiasi Kerberos/GSSAPI lalu fallback) — container tetap "healthy" DAN outbox dispatcher terbukti jalan (query `SELECT ... FROM OutboxMessages` sukses tereksekusi berulang di log yg sama) — diduga KUAT benign/pre-existing (bukan diperkenalkan perubahan sesi ini), TAPI belum diinvestigasi tuntas krn di luar scope tiket — dicatat apa adanya sesuai disiplin "jangan diam2 lewati temuan", bukan diasumsikan aman tanpa catatan |
| D30 | 2026-07-22 | Review ulang AC VOK-H4-E3 sebelum tutup tiket (baca `TICKETS.md` §H4-E3 literal, bukan cuma ingatan): 2 AC selain DLQ ternyata BELUM pernah dibuktikan sesi ini — (a) "template email render 5 jenis, konsisten header/footer, plain-text fallback ada" TAK PUNYA test otomatis sama sekali (`grep EmailTemplateRenderer` di `tests/` = nol hasil sebelum ini); (b) "email gagal kirim, retry, tidak menduplikasi notifikasi in-app" belum eksplisit diverifikasi | (a) Ditambah `tests/Vokasia.Tests/Email/EmailTemplateRendererTests.cs` — 2 `[Theory]` (konsistensi header/footer, plain-text fallback) × 5 template (termasuk `ExportReady`/`InvoiceIssued` yg belum ada consumer produksi — SENGAJA tetap direndertes supaya AC "5 jenis" benar2 kelimanya, bukan cuma 3 yg kepakai) + 1 `[Fact]` HTML-encoding nama siswa. (b) Diverifikasi via BACA KODE (bukan test baru) ketiga consumer email (`MentorInvitedConsumer`/`JournalReminderEmailConsumer`/`GhostingAlertEmailConsumer`): KETIGANYA murni kirim-email SAJA, TAK SATU PUN menulis ke tabel `Notifications` dlm method `Consume`-nya (penulisan Notification in-app SELALU consumer/proses TERPISAH - mis. `FlagGhostingStudents` cron sendiri, bukan consumer email ini) — jadi retry consumer email SECARA STRUKTURAL tak bisa menduplikasi Notification (tabel itu tak disentuh sama sekali oleh consumer yg retry), independen dari idempotency Notification yg sudah dibuktikan terpisah sejak H4-E1 | **[BLOKER JUJUR, sesi berhenti di sini]**: percobaan mengeksekusi+PROMPT-D `EmailTemplateRendererTests.cs` baru (via pola `mcp__Windows-MCP__PowerShell` yang sama dipakai sepanjang sesi) GAGAL BERULANG dgn error infrastruktur "`claude-sonnet-5[1m] is temporarily unavailable, so auto mode cannot determine the safety of mcp__Windows-MCP__PowerShell`" — dicoba ULANG 8× dgn jeda makin panjang (total >10 menit), dikonfirmasi BUKAN spesifik tool ini (panggilan `mcp__Desktop_Commander__get_config`, tool BEDA, read-only, kena error IDENTIK) — outage classifier tingkat platform, bukan bug kode/keputusan desain. **TIDAK diklaim lolos test** (SOUL.md ditegakkan literal walau di bawah tekanan waktu instruksi user "sudahi setelah 1 task") — file test SUDAH ditulis+ditinjau manual (logika benar: 5 template semua pakai `Layout()`/`LayoutText()` privat yg SAMA, jadi header/footer PASTI konsisten by construction) tp status EKSEKUSI NYATA + siklus PROMPT D **belum terbukti, harus jadi hal PERTAMA sesi berikutnya** sebelum dianggap valid. Semua verifikasi LAIN di D29 (full suite 146 passed, docker rebuild+restart, PROMPT D DlqReplayTests) SELESAI DUKUNG SEBELUM outage ini mulai — tidak terdampak |

| D31 | 2026-07-22 | User minta redesain "tema sekolah" khusus role guru & murid: warna #4ED7F1/#FFFDF6(bg)/#6FE6FC/#A8F1FF, cocok layar hp + internet 3G. DESIGN.md status BEKU (D20) -> wajib change control, bukan edit diam2. Dikonfirmasi eksplisit ke user via AskUserQuestion: (a) ganti sistem desain penuh (bukan cuma preview) vs (b) cakupan halaman = /student + /mentor + publik (login/verify/portofolio/mentor-invite), TIDAK termasuk /app /sa (dashboard desktop institusional tetap anchor hue 222 lama) | Palet SUMBER (4 warna user) semua terang (L 82-99%), kontras vs satu sama lain cuma 1.2-1.7:1 -- TAK CUKUP dipakai sbg teks (WCAG 4.5:1). Solusi: 4 warna asli DIPERTAHANKAN APA ADANYA sbg surface/primary-muted/accent-bright/accent-light (semua non-teks -- bg, dekorasi, highlight), sedangkan ink/primary/border/focus DITURUNKAN pd hue yg sama (~213 cyan, turunan dari #4ED7F1) di lightness lbh rendah spy teks & elemen fungsional tetap kontras terverifikasi programatik (OKLCH->sRGB->WCAG via coloraide): ink 18%/213 vs bg=18.4:1, ink-muted 48%/213=6.3:1, primary 50%/213=5.6:1 (dan 5.5:1 sblknya utk primary-ink di atas primary), border 65%/213=3.1:1 (>=3:1 non-teks), focus 55%/213 (C0.18)=4.47:1. Diimplementasi scoped via `[data-theme="sekolah"]` (BUKAN menimpa `:root`) supaya /app /sa institusional tak tersentuh -- diterapkan sbg attribute di root div (student)/mentor layout.tsx + `<main>` di login, verify/[code], p/[slug], mentor-invite. Genre/tone TIDAK berubah (modern-minimal/utilitarian tetap, krn app fungsional PKL bukan landing konsumer) -- hanya anchor hue/lightness map. Font Geist/Geist Mono dipertahankan (tak ada sinyal user minta ganti font, hallmark pre-flight rule: preserve font stack). RAG status (green/amber/red) dicek ulang vs bg baru, masih lolos (6.6/5.6/6.4:1), tak diubah. Stripe dekoratif header (gradient accent-bright->accent-light, 3px, CSS-only) + hover nav item pakai primary-muted -- nol asset/font tambahan, sesuai anggaran 3G (NFR-PERF-05) | `frontend/src/app/globals.css` (+`[data-theme="sekolah"]` light+dark block, +2 token `--color-accent-bright`/`--color-accent-light` di `@theme inline` & `:root` fallback institusional), `(student)/student/layout.tsx`, `(mentor)/mentor/layout.tsx`, `login/page.tsx`, `verify/[code]/page.tsx`, `p/[slug]/page.tsx`, `mentor-invite/page.tsx` (attribute `data-theme="sekolah"` ditambah, tanpa mengubah logic/komponen lain). DESIGN.md diupdate dgn subsection baru "Tema Sekolah (scoped)". Belum dijalankan `next build`/`tsc` end-to-end di sesi ini krn sandbox terbatas -- verifikasi dilakukan via baca ulang JSX manual + kalkulasi kontras terprogram (dilampirkan angka di atas), BUKAN diklaim lolos build nyata tanpa bukti (SOUL.md) |

| D32 | 2026-07-23 | Sesi baru (user minta mulai ulang dari awal utk bangun context window, lalu eksplisit minta lanjutkan 2 carry-over D30/D31 dulu sebelum ticket baru) — outage platform D30 (`Windows-MCP PowerShell`/`Desktop_Commander` unavailable) dicek ulang PERTAMA: **resolved**, `dotnet --version` (10.0.302) & `bun run build` langsung jalan di host, tak perlu lagi workaround rsync-ke-non-FUSE dari sesi D27 (repo dibuka langsung via `mcp__Windows-MCP__PowerShell` di `D:\Web\Vokasia`, bukan mount Linux) | (1) `EmailTemplateRendererTests.cs` (ditulis D30, nol eksekusi) dijalankan: 11/11 hijau. **PROMPT D dijalankan sungguhan** (bukan lagi diklaim "logika benar via baca kode" spt D30): `FooterText` diganti string temp non-marker → re-run → **10/11 GAGAL persis prediksi** (`Assert.Contains() Failure: Not found: "Platform Manajemen PKL SMK"`, 1 lolos = Fact HTML-encoding yg memang tak sentuh footer) → dikembalikan tepat semula → re-run → 11/11 hijau lagi. Full suite non-filtered: **157 passed, 3 skipped (sama persis 3 lama), 0 failed** (naik dari 146+3 di D29, +11 test baru ini). (2) D31 "tema sekolah": `bun run build` dijalankan (belum pernah di D31 krn sandbox terbatas) — Next.js 16.2.10 compile+TypeScript+11/11 static page generation semua hijau, `$LASTEXITCODE=0` dikonfirmasi eksplisit (bukan cuma baca output teks — sempat ada sinyal palsu "Status Code: 1" dari wrapper saat output di-pipe ke `Select-Object`, diverifikasi ulang tanpa piping utk pastikan bukan gagal asli). (3) Git hygiene: `git diff --stat` per file utk 8 file D31 sebelum stage — semua pola insertion-only/proporsional wajar (BUKAN pola noise CRLF/LF insertion==deletion==total-baris dari D16/D19) → aman di-`git add` eksplisit satu-satu (bukan `-A`). File di luar scope (`HANDOFF-NEXT-SESSION.md`, `Vokasia.tldraw`, `backend/.idea/**`, `stitch-prompt-*.md`, `.claude/`) SENGAJA dibiarkan untracked, bukan ikut ke-stage | Ketemu 2 hal operasional baru: (a) `.claude/settings.json` (dibuat sesi sebelumnya utk pairing OmniRoute→VSCode, berisi token akses di URL) **BELUM ter-`.gitignore`** — celah nyaris commit secret, langsung ditambal (`.gitignore` +`\n.claude/settings.json`) SEBELUM staging apa pun lain, commit terpisah kecil duluan. (b) `.git/index.lock` DAN `.git/HEAD.lock` stale (dua lock berbeda, bukan cuma index spt D16/D27) muncul 2× berturut saat commit pertama sesi ini — dikonfirmasi `Get-Process git` kosong tiap kali sebelum `Remove-Item`, pola identik sesi lalu, bukan penyimpangan baru | Commit `0692f1b` (chore: gitignore), `295e7d7` (theme: data-theme=sekolah, 8 file/120+/13-). **Carry-over D30+D31 SEKARANG TUNTAS PENUH** (test dieksekusi nyata + PROMPT D + build hijau, bukan lagi klaim via baca kode). Lanjut VOK-H5-E1 sesuai urutan `TICKETS.md` (tak ada ticket baru di luar sprint 21-ticket yg diminta user) |

## Backlog Minggu 2+

(kosong — belum ada ide yang ditolak dari scope sprint)
