# Release UX evidence

Run Lighthouse against `/student` and `/p/{slug}` at 360px mobile emulation.

Targets: performance >=85, accessibility >=90, no horizontal overflow, visible focus,
keyboard navigation, 44px touch targets, and reduced-motion behavior.

Results are **NOT VERIFIED** until captured on the developer machine.
